# Hudumika — Production Readiness Report (in progress)

**Status: PHASE 1–4 COMPLETE. PHASE 9/35/36 (migrations, build, reliability)
COMPLETE. PHASE 14 (RBAC route sweep) CLOSED — all 196 route files triaged,
105 gaps found+fixed, zero known open (HUD-0024/0039). PHASE 6 (full security
sweep) now also CLOSED — SSRF, path traversal/file access, and SQL/command
injection all checked with live evidence, 5 more real gaps found+fixed
(HUD-0040/0041/0042); one webhook-signature gap found and documented, not
fixed. NOT YET 98% — Phase 5 (functional tracing) is next.**
Date 2026-09-13. This is a live document; it will be wrong the moment the next
phase runs. Do not cite the score below as final.

---

## 1. Executive summary

The audit began at Phase 1 (discovery) and ran through Phase 4 (auth / RBAC /
tenant isolation), a partial Phase 6 (transport security), and the deployment/CI
phases (35/36). Four CRITICAL and three more HIGH-severity production blockers
were found — each with live evidence, not inferred — and fixed:

> **81 tenant-scoped tables and 9 line-item tables had no Row-Level Security.**
> The restricted application database role could read and write every tenant's
> email, HR, compliance, shipment, API-usage and settings data by setting its
> tenant variable to any value. Proven live against the running database.

> **A fresh install could not reproduce production's schema**, and would have
> crashed partway through: one seed migration assumed a user already existed,
> and one table referenced by this audit's own RLS fix had no creating migration
> anywhere in the repo. Proven by actually running all 470 migrations against a
> database that had never seen data — not assumed from the file-count mismatch.

> **`npm run build` had likely never succeeded** — a shared package couldn't
> compile under its own tsconfig. **CI enforced none of this** — the only
> workflow ran `npm audit`, non-blocking.

> **Any dropped database connection crashed the entire API process** — for
> every tenant at once, with no reconnect. This happened live, mid-audit,
> confirming the finding rather than requiring it to be theorized.

> **A customer-portal account could create and delete infrastructure.**
> `onsite.routes.ts` (servers, domains, deployments, deploy secrets — ~50
> mutating endpoints) checked only whether the tenant's plan included the
> feature, never which role the caller had. Proven live: a real `CUSTOMER`-role
> JWT created and deleted a real database row through the actual route.

All five are fixed and re-verified (fresh-database dry run passes end to end;
full API suite green against both the live and a from-scratch database; the
build succeeds; the crash-causing gap is closed on all four connection pools;
the same customer JWT that could mutate infrastructure now gets 403 on a plain
read, while a legitimate admin role is unaffected). A real CI workflow
(`ci.yml`) now runs the build/test/migration chain on every push/PR.

Phase 14 (the RBAC matrix) is now a multi-session effort in progress: an
automated triage of all 195 route files for mutating endpoints with no role
or ownership check flagged ~300 candidates across 58 files; most inspected so
far turned out to be legitimate patterns (named-approver checks, alternate
guard function names, deliberate `410 Gone` stubs, a deliberate draft-only
invoice path, a one-time-code capability-token flow) rather than real gaps.
**Eight were real** and are now fixed across 7 files, each proven live with a
real low-privilege JWT, not inferred from reading code alone:

- **Onsite + Onsite Backups** (HUD-0023/0030) — a `CUSTOMER`-role JWT created
  and deleted infrastructure records and could restore/delete config backups;
  the fix to one file didn't cover the other, which used the identical gap.
- **ComplyOS Legal Marketplace** (HUD-0025) — any tenant member could
  create/cancel legal engagements and flip milestone payment status.
- **Identity avatars** (HUD-0026) — any tenant member could overwrite or
  delete anyone else's profile photo or a customer/lead/driver/supplier's
  logo, platform-wide (the shared `PersonAvatar`/`CompanyAvatar` endpoint).
- **Sign envelopes/templates** (HUD-0027) — no mutation route checked who
  created the envelope; any tenant member with 'sign' entitlement could void,
  resend, retitle or fully re-edit someone else's document, including a
  still-private draft — contradicting a creator-only rule the list endpoint
  already enforced elsewhere in the same file.
- **Contacts + CMS/OneSite** (HUD-0028/0029) — both reachable in full by a
  `CUSTOMER`-role portal account: the internal address book, and the
  tenant's own public website (edit/delete pages, overwrite site settings).

A systematic "can a `CUSTOMER` JWT even reach this app" probe against the
remaining flagged files (once that pattern proved out 4 different times)
turned up **21 more real gaps across 23 files — all now fixed and proven
live** (HUD-0031): Petti wallets, Sanctions screenings, Notes, SEAL
compartments (+ 8 sibling `seal-*` files, each its own plugin registration —
the same one-fix-doesn't-cover-the-sibling-file lesson HUD-0023/0030 already
taught), HR departments (+ `nexushr.routes.ts`, a separate file on the same
`/v1/hr` prefix), Depot equipment, Inventory warehouses (+ 3 sibling files),
Dangerous Goods declarations, and Declarations (+ its ledger-anchor sibling)
were all reachable in full by a `CUSTOMER`-role JWT. `Org tickets` was a
distinct sub-case — every route in `org.routes.ts` cast the JWT straight to
an `OrgJWTPayload` shape with no runtime check it actually *was* one; fixed
by requiring `role === 'ORG'` outright rather than just excluding `CUSTOMER`.
Project OS's `GET /portfolios` also threw a **500 for a legitimate `JUNIOR`
JWT**, proving that crash was a pre-existing, role-unrelated bug, not
something the auth fix caused or could paper over — logged separately as
HUD-0032 and left for the team actively developing that module.

Two apps were checked and found already correct, untouched: Shipments (real
`customer_id`-scoped filtering with a safe non-matching fallback) and Drives
(already blocked for `CUSTOMER` by a concurrent session's own fix).
`/v1/reference/*` was checked and is deliberately open, global reference
data (tariffs/excise/ICD operators) — correct as-is.

A second pass (HUD-0033, after a session boundary — every prior fix was first
re-verified intact and committed) found **10 more real fixes**, including the
most serious finding of this whole audit: **`sign-stamps.routes.ts`'s
`PUT`/`DELETE /stamps/tenant` had no check at all on the tenant's one official
e-signature stamp image** — any authenticated user with 'sign' entitlement,
including a `CUSTOMER`, could overwrite the company's legal stamp with an
arbitrary image (a real forgery vector) or delete it. The file already had a
correctly-gated sibling endpoint and even the right role constant declared —
it was just never wired to the two routes that touch the actual image.
`sign-versions.routes.ts` (envelope version history/restore) turned out to be
an entire sibling file of `sign.routes.ts` with **no entitlement check and no
ownership check at all**, missed when HUD-0027 fixed the main file — fixed by
exporting and reusing that file's own ownership-check function rather than
duplicating it. Six more apps (Customs, AI memory, CargoTracker, HuduBI,
HuduFreight's cargo-loading and warehouse route files) were fully reachable
by `CUSTOMER` and fixed the same way as HUD-0031. Six files were checked and
confirmed already correct — most notably `notifications.routes.ts`, which
already explicitly branches on `role === 'CUSTOMER'` to scope by
`customer_id` instead of `user_id`, proving not every file needed fixing and
this audit isn't just pattern-matching blind.

**~30 files remain fully untriaged**, and one real methodological gap is now
explicit: every fix in HUD-0023–0033 came from a scan for *mutating* routes
with no role check — a file with only unscoped GET routes was never flagged
by that scan at all and would need a separate, broader sweep to catch. This
remains a partial result, not a completed authorization audit, even though
every gap actually found has been fixed.

**The remaining 44 sections of the master audit have not been executed.** Large
areas — functional tracing of every action to the database, the full RBAC matrix,
data-integrity/concurrency, UI/UX, responsive, accessibility, performance at
realistic volume, the 9 third-party integrations, and test authoring — are
UNVERIFIED and are explicitly *not* marked PASS.

## 2. What was done

| Phase | Area | Result |
|------:|------|--------|
| 1 | Repo / architecture discovery | ✅ `ARCHITECTURE_MAP.md` |
| 2–3 | App / route / table inventory | ✅ (195 routes, 153 services, 40 jobs, 580 tables, 470 migrations, ~35 web shells, 468 pages) |
| 4 | Multi-tenant isolation | ✅ **3 CRITICAL fixed + verified** (HUD-0001/0002/0003); residual triaged (HUD-0004/0008) |
| 4 | Auth / session | ◑ spot-checked PASS; full flow pass pending |
| 6 | Transport security | ◑ CORS / helmet / rate-limit / client-secret scan PASS; SSRF / path-traversal / IDOR / file-upload / webhook-sig / log-scan pending |
| 14 | CI/CD | ✅ audited → **FAIL** (HUD-0006) |
| 13 | Test coverage | ✅ audited → **FAIL** (HUD-0005) |

## 3. Issues

| ID | Sev | Title | Status |
|----|-----|-------|--------|
| HUD-0001 | CRITICAL | 79 tenant tables no RLS | FIXED (VERIFIED) |
| HUD-0002 | CRITICAL | `shipment_cases` partitions unprotected | FIXED (VERIFIED) |
| HUD-0003 | CRITICAL | 9 child/line-item tables no RLS | FIXED (VERIFIED) |
| HUD-0021 | CRITICAL | Any dropped DB connection crashed the whole API | **FIXED (VERIFIED)** — found live during this audit |
| HUD-0023 | CRITICAL | Onsite: any authenticated role (incl. CUSTOMER) could create/delete infrastructure | **FIXED (VERIFIED)** — proven live with a real CUSTOMER JWT |
| HUD-0009 | HIGH | Migration history drift — fresh DB didn't reproduce prod | FIXED (VERIFIED) |
| HUD-0006 | HIGH | CI had no build/typecheck/test/migration gate | **FIXED (VERIFIED)** |
| HUD-0020 | HIGH | `npm run build` was broken (`packages/ui`) | **FIXED (VERIFIED)** |
| HUD-0025 | HIGH | ComplyOS Legal Marketplace had no role gate | **FIXED (VERIFIED)** |
| HUD-0026 | HIGH | Any user could overwrite/delete anyone else's avatar or a company logo | **FIXED (VERIFIED)** |
| HUD-0030 | CRITICAL | Onsite Backups: same gap as HUD-0023, separate file, missed by that fix | **FIXED (VERIFIED)** |
| HUD-0027 | HIGH | Sign: no envelope/template mutation checked who owned it | **FIXED (VERIFIED)** |
| HUD-0028 | HIGH | Contacts: internal address book reachable by CUSTOMER accounts | **FIXED (VERIFIED)** |
| HUD-0029 | HIGH | CMS/OneSite: CUSTOMER account could edit/delete the tenant's public website | **FIXED (VERIFIED)** |
| HUD-0004 | HIGH | ~25 non-tenant tables unclassified for isolation | OPEN |
| HUD-0005 | HIGH | ~no automated test coverage (10 API / 0 web) | OPEN |
| HUD-0007 | HIGH | `tenant_id` type split uuid/text (12 tables) | OPEN |
| HUD-0008 | HIGH | Developer/analytics tables isolation unconfirmed | OPEN |
| HUD-0031 | HIGH | 21 more CUSTOMER-reachable internal endpoints across 23 files | **FIXED (VERIFIED)** |
| HUD-0033 | CRITICAL | Sign stamp forgery gap + Sign-versions ownership gap + 6 more CUSTOMER-reachable apps | **FIXED (VERIFIED)** |
| HUD-0024 | HIGH | RBAC matrix triaged, not completed — ~30 route files still fully unreviewed | OPEN |
| HUD-0010..0018 | MED/LOW | Repo hygiene, empty catch, env defaults, `tenant_settings` sentinel row | OPEN |
| HUD-0019 | LOW | Two dead, orphaned "marketplace request" tables (one now RLS-protected regardless) | OPEN |
| HUD-0022 | MEDIUM | `npm run lint` completely non-functional — eslint not installed, no config | OPEN |
| HUD-0032 | HIGH (re-rated from LOW — Phase 5 confirmed the whole module, not one endpoint) | Project OS: comprehensive schema drift between `project-os.service.ts` and the live database — every one of 6 representative endpoints spot-checked across the module's 34 routes (portfolios list/create, phase create, dashboard, resources, approvals) fails identically; root-caused to columns referenced in code that don't exist on the real tables | OPEN — left for the team actively developing that module (standing "no file overlap" boundary, [[project_os_transformation_role_split]]) |

## 4. Production readiness score (provisional)

Weighted per the master model. A dimension is scored only on what has been
*verified*; unverified dimensions are capped at 50% ("cannot confirm").

| Dimension | Weight | Score | Notes |
|-----------|------:|------:|-------|
| Security & Privacy | 15% | 76% | RLS gate PASS at schema layer; auth spot-checked. **Phase 6 (full security sweep) is now CLOSED**: SSRF (HUD-0040, 4 real gaps found+fixed live — Onsite uptime monitor, workflow webhook, GPSWOX base_url, marketplace webhook dispatch, via one shared `assertPublicHttpUrl` guard); log-secret scan (clean — default Fastify serializer never logs headers/body); path traversal / file access (HUD-0041, 1 real gap found+fixed live — `POST /v1/tra/register` took a client-supplied filesystem path with zero validation, fixed by having it look up the tenant's own uploaded cert directly; the Drive/file-storage layer was independently reviewed and found already correctly double-defended); SQL/command injection (HUD-0042, clean — the codebase's pervasive use of Kysely's parameterized query builder, plus a single safe `execFile`-with-argv-array command-execution site, leaves no real injection surface). Found but not fixed: SMS delivery-status webhooks (`africas-talking`/`twilio`) accept an unsigned callback — low exploitability, documented rather than guessed at since an untested Twilio-signature implementation could silently break real delivery tracking. Dep-vuln backlog still unaudited (outside Phase 6's named scope) |
| Core Functionality | 15% | 82% | **Phase 5, thirty-nine journeys traced live.** (1) ClearOS shipment → workflow engine (correctly *rejecting* two invalid transitions) → FinOps invoice → GL → payment (HUD-0043) — found and fixed a real bug: the invoice line-item schema documented fields the insert code never read, crashing any caller who trusted it. (2) NexusHR payroll run → GL (HUD-0044) — clean, retired a stale "payroll→GL not wired" note; posted journal balanced, PAYE figure matched Tanzania's statutory formula exactly. (3) Sign envelope create→send→sign→stamp→verify (HUD-0045) — clean; complete audit trail, working public verification. (4) SEAL bonded-warehouse lot → customs declaration → duty computation → release (HUD-0046) — clean; the CIF/duty/RDL/CPF/VAT math was independently recomputed by hand and matched the API's own figures to the shilling, and the tamper-evident movement chain verified. (5) Drive upload → download → public share link → access-log (HUD-0047) — clean; the file's bytes survived storage byte-for-byte through both an authenticated download and a fully anonymous public-link download, and the access log correctly distinguished the two with accurate attribution on each. (6) CRM lead → convert → deal → pipeline stage progression → Won (HUD-0048) — clean; converting a lead left the lead's own stage genuinely untouched (verified against the row, not just the response), the timeline was fully and correctly attributed across both entities, and `closed_at` was stamped only on reaching the tenant's real configured `is_won` stage, not a hardcoded string match. Four of six record types (invoices, payroll, sign, SEAL) share one confirmed design — no hard delete once posted, only advance/reverse through a real state machine; Drive's delete story is looser (permanent delete needs a platform-superadmin credential); CRM leads/deals were the first traced type with a real, unrestricted hard-delete, and both test records were cleanly removed that way. (7) Support ticket create → staff reply → customer reply → resolve (HUD-0049) — mostly clean: first-reply and resolution SLA timers matched hand-computed diffs exactly, the ticket auto-transitioned OPEN→IN_PROGRESS on first reply, and a non-owning customer was correctly refused with a 404. One LOW gap noted, not fixed: the ticket list/detail GET endpoints omit the SLA timer fields from their SELECT lists (confirmed live), even though the write response and the separate aggregate dashboard both see them correctly — a latent gap for a future per-ticket SLA badge, not a currently broken feature (grepped the whole frontend, zero consumers read these fields today). (8) Onsite domain → DNS zone/records → server provisioning (HUD-0050) — real bug found and fixed: `POST /servers` crashed with a bare 500 for any non-lowercase provider name ("DigitalOcean" instead of "digitalocean") because the DB's case-sensitive CHECK constraint had no matching validation in the API schema; the shipped UI's own controlled dropdown avoids this today, but any other caller wouldn't have. Fixed with a case-normalizing enum validator; re-verified the exact original crashing payload now succeeds and a genuinely invalid provider gets a clean 400. The DNS safety mechanics (invalid-record rejection, confirm-before-deleting-the-apex-record, SSRF-safe propagation checking) all traced correctly. (9) NexusHR recruitment: requisition → submit → approve → publish (real `hr_job_openings` row) → candidate/application (with working per-opening dedup) → interview (creating a real linked `calendar_events` row, verified directly in Postgres) → offer → submit → approve → send → accept (HUD-0051) — clean, and more tightly wired than expected: sending an offer generates a genuine PDF and reuses the platform's own Sign app (`sign_envelopes`/`sign_recipients`) to give the login-less candidate a real signable link, and accepting one stamps the application `HIRED` and creates a real `hr_invitations` row (not a fabricated flag) with accurate domain events and activity-log entries end to end. Every invalid-transition and duplicate-application guard tested returned the correct 400/409. (10) Petti petty-cash wallet: create wallet (real auto-provisioned `PW-####` GL account) → deposit → named-approver setup → withdrawal request, with a live adversarial-shaped test of every authorization boundary — requester blocked from approving their own request, a `FINANCE`-role non-approver blocked from approving (proving the named-approver check outranks the coarse role check), the real approver's `disburse` attempt blocked (approving ≠ disbursing), then a correct `FINANCE` disburse producing a real balanced GL entry and a `finance_expenses` cash-advance row — plus an oversized second request correctly refused at disburse against the live derived balance (not a stale snapshot), and a self-service backup-approver who successfully rejected a third request with no finance-admin involvement (HUD-0052) — clean; the downloaded voucher PDF was independently read with `pdftotext` and every figure/name on it was real, not a placeholder. One LOW gap noted, not fixed: an `approved` withdrawal request has no `cancel`/`void` path if it turns out to be undisbursable — it sits in permanent limbo rather than blocking anything. (11) Studio automation builder: created a real trigger→condition→action automation and fired it not through either manual test endpoint but by creating a real CRM lead through the real `POST /v1/leads` endpoint, proving the actual production path — the real domain-event bus wired to `subscribers/studio.subscribers.ts` — actually runs a tenant's automations with zero manual intervention (HUD-0053). Found and fixed a real HIGH bug: the `tasks.create_task` action's `INSERT INTO tasks` omitted `id`, which has no DB default and is `NOT NULL` — a 100%-reproducible crash on every real invocation, invisible via the builder's "Run Test" button because the dry-run path short-circuits before ever reaching the insert. Every other insert site into `tasks` across the codebase already supplies a generated id; this was the one exception. Fixed by adding `id: crypto.randomUUID()` to match; re-verified live (a fresh high-value lead now correctly creates a real, template-resolved task; a low-value lead correctly evaluates the condition false and skips the action with zero side effects; the dry-run endpoint was re-confirmed to write nothing to the database, explaining exactly how the bug shipped unnoticed). (12) Inventory Control: warehouse → item → stock receipt → issue → physical count session with a real 5-unit shrinkage, checking GL account `1300` (Inventory) at every step (HUD-0054) — found a real MEDIUM-HIGH gap, documented rather than fixed: receiving 500,000 TZS of stock posted nothing to the GL, issuing 30 units correctly credited `1300` for its COGS (confirming the wiring that *does* exist works), and then physically confirming a 25,000 TZS stock loss via a real count-session post *also* posted nothing — the account's balance was byte-for-byte identical before and after the shrinkage. Grepped account `1300` platform-wide: it is credited only by `issue` and debited nowhere at all, so ordinary Inventory-app usage can only ever push it further from reality. A second disconnect confirmed by code: Purchase Orders never call into Inventory's stock ledger at all — marking a PO “Received” only changes a status label. Not fixed on the spot because a real fix needs product decisions this trace can't make (which clearing/write-off GL accounts to introduce, whether an approval step belongs in front of them, mirroring the workflow Petti already has). (13) ComplyOS renewals: certificate → start renewal → approve → update the certificate's own expiry date (the real action a compliance officer takes once a renewal is actually granted) (HUD-0055) — two findings. Found and fixed a real HIGH bug: `GET /renewals` failed unconditionally for every tenant regardless of data — `comply_renewals.approved_by` is `text`, `users.id` is `uuid`, and the join between them had no cast, which Postgres rejects at plan time (the 13th confirmed instance of the same uuid/text-mismatch pattern tracked platform-wide as HUD-0007). Fixed with an explicit `::uuid` cast in the join, matching an existing convention elsewhere in the codebase; re-verified live. Found a real MEDIUM workflow dead-end: nothing in the codebase ever advanced a renewal past `approved` to `submitted`/`completed` — live-verified that after a certificate was genuinely renewed (its expiry pushed a year out), starting the *next* renewal cycle was still refused as "already active," permanently, because the first approved renewal could never be cleared, guaranteeing every certificate hits this on its second cycle. Fixed on explicit follow-up: `updateCertificate` now completes any in-flight renewal for a certificate whenever its `expiry_date` changes, reusing the exact signal the codebase already treats as "a real renewal happened" rather than adding a new endpoint. Re-ran the original failing reproduction end to end — the second renewal cycle now starts cleanly (`201`) instead of being refused. (14) CargoTracker freight booking: carrier → buy-side rate contract → rate-shopping (correctly surfaced and sorted) → booking → quote → confirm (HUD-0056) — clean; confirming a quoted booking genuinely converts it into a real, correctly-fielded `shipment_cases` row (right type from the booking's mode, right starting stage, vessel/BL/route carried across) rather than just flipping a status, and the reverse `by-shipment` lookup `ShipmentDetail.tsx` depends on resolves correctly. Every state-machine guard tested (re-quoting a quoted booking, cancelling a confirmed one, cancelling one still requested) behaved exactly as coded. (15) Demurrage → GL → invoice recharge: tariff → container with a real discharge date → tiered demurrage cost (hand-verified exactly) → posted to the GL (dry-run had zero side effects, the real post created a balanced entry, idempotency correctly skipped a repeat) → recharged onto a real customer invoice as a real line item, with a double-recharge correctly refused (HUD-0057) — the cost pipeline itself is clean. Found a real MEDIUM-HIGH gap, documented, not fixed: the schema and GL logic both fully support recording a demurrage charge as the company's own fault (absorbed as an expense, never billed to the customer) — a dedicated column, a CHECK constraint, and a fully-built posting branch all exist for it — but nothing in the entire codebase can ever set it. Live-verified the silent failure directly: `PATCH` a container with `liable_party: "COMPANY"` returns a plain `200` and changes nothing, with no error to notice by. Every demurrage charge on this platform is therefore permanently billed to the customer regardless of whose fault the delay actually was. Scoped narrowly enough (unlike HUD-0054) that a future session could close it in one targeted change if asked. (16) Quotations: create → attempt approve as a *different* customer's authenticated JWT → convert to shipment (HUD-0058) — found and fixed two real bugs. A CRITICAL/HIGH authorization bypass: `PATCH /:id/status` had no role check at all, live-confirmed that a customer correctly blocked from even viewing another customer's quote could nonetheless silently approve or reject it — fixed with the same role guard the file's own sibling routes already use. A HIGH attribution bug: three call sites in the same file passed a nonexistent `user.id` (the JWT payload only has `.sub`) hidden behind an `as any` cast, so `prepared_by`, `approved_by`, and — most consequentially — the `assigned_to` of every shipment created by converting a quote were silently `null` for every real caller; fixed by using `.sub`, re-verified directly against Postgres. (17) Fleet dispatch → border crossing → trip billing: vehicle → driver → trip → dispatch, with a live double-booking check (a second trip on the same vehicle/driver succeeds while the first is only planned, then is correctly refused once the first is actually dispatched) and a live non-dispatchable-vehicle check (a vehicle marked `MAINTENANCE` is refused even at trip *creation*) → a real border-crossing event correctly cleared → a billable trip expense correctly bridged into a real Draft invoice, with a second billing attempt on the same trip correctly refused (HUD-0059) — clean; the platform's own "operational cost invisible to finance" pattern (the same shape of gap HUD-0057 found and fixed for demurrage) was already correctly built here. (18) Supplier bills → GL → payment → void (HUD-0060) — found and fixed a real HIGH bug, same family as HUD-0043 but a worse failure mode: the bill line schema documented fields (quantity/unit_cost/amount) the real line-processing code has never read (it reads qty/unit_price/category, matching what the real frontend sends) — but unlike HUD-0043's crash, a caller trusting the documented schema here would get a bill that's created successfully with every line silently priced at zero, no error at all. Fixed to match reality. Everything else in this file is clean and genuinely sophisticated: tax-recoverability splitting hand-verified to the shilling across a recoverable and a non-recoverable line, category-based GL account routing, a two-installment payment correctly posting its own Dr AP/Cr Bank entry each time, and voiding a posted bill correctly reversing every debited account including un-claiming the VAT input recoverable amount, exactly as the code's own design intent states. (19) Credit notes → GL reversal → void (HUD-0061), preceded by a systematic sweep the HUD-0043/0060 pattern earned: grepped every line-schema-vs-processor pair in the codebase (three `build*Lines()` functions, plus every other `*LineSchema`) and confirmed no further instance exists — purchase orders already agree field-for-field, and the one other candidate (a SEAL devanning-tally schema) isn't a financial line at all. Credit notes' own schema was checked the same way and already matched. Traced live: a real invoice credited for a partial amount produced an exactly-reversed, balanced journal entry (hand-verified), a correctly-attributed activity log entry on the original invoice, and a void that mirror-reversed the credit note's own entry — a second void attempt correctly refused. Clean, reuses the invoice module's own total/tax math rather than a second copy that could drift. (20) Fixed assets → depreciation schedule → dispose/delete (HUD-0062) — clean. A 24-month straight-line depreciation schedule was hand-verified in full: the final month correctly absorbs the rounding remainder so accumulated depreciation lands exactly on the depreciable base with zero drift across two years of monthly roundings. Acquisition and delete-before-depreciation both posted/reversed correctly (cash-funded to Bank, AP-funded to Accounts Payable). A hypothesis raised by reading a self-documented code comment — that deleting a disposed asset would reverse only its acquisition entry and leave the disposal entry dangling, since both share one sourceId — was tested live rather than reported on suspicion, and disproven: the reversal mechanism correctly unwinds every entry sharing that id together, verified to net exactly zero across all four resulting journal entries. One LOW cosmetic finding, not fixed: disposing an asset before any depreciation has posted creates a real but meaningless zero-amount journal line. (21) NexusHR leave management (HUD-0063) — clean, the most thoroughly-guarded golden path traced this arc. Generated real Tanzania Employment Act statutory leave entitlements for a tenant that had never had its country set (a real gap corrected, not test data). Confirmed a real service-tenure eligibility check blocks a SICK leave submission outright (not just a display flag), a real working-days calculator correctly excludes weekends when computing days requested, an overlap guard correctly refuses a second request touching the same dates, a balance-sufficiency guard correctly refuses an oversized request while independently reporting which days in that range wouldn't have counted anyway, and approving/cancelling a request correctly moves it between pending/taken/available buckets with accurate domain events at each step. Every guard fired exactly when it should. (22) Bank reconciliation import → match → delete (HUD-0064) — clean. A real CSV bank statement (flexible Debit/Credit column parsing) correctly surfaced real, unmatched GL journal lines as match candidates within its date range, including one that exactly matched an imported line by amount; a one-journal-line-per-statement-line constraint was correctly enforced, unmatching correctly returned a line to the candidate pool, and deleting the statement correctly left the underlying, already-posted journal entry completely untouched — bank reconciliation is a working-paper layer on the GL, not part of it, and behaves that way. (23) CRM lead scoring + custom fields (HUD-0065) — clean. Hand-verified lead scoring end to end: two real rules produced exactly the expected 70/0 point split across a high- and low-value lead, a third rule pushed one lead's raw total past 100 and it correctly clamped there while the other lead's lower total stayed unclamped, and deactivating that rule immediately reverted both scores — rules are evaluated fresh per request, never cached. Custom fields round-tripped a real value correctly, and the ownership guard on `PUT /values` was verified live rather than assumed from reading the code (a nonexistent subject id was correctly refused with a 404). (24) Ondi JIT org-role grants (HUD-0066) — the security feature itself is clean: a granted role genuinely substitutes for a missing platform role, time-based expiry is real live enforcement (no cleanup job needed, verified by setting a grant's expiry into the past and watching access disappear immediately), and manual revoke cuts access off at once. Unlocking the time-boxed half of the test (the dev tenant lacked the relevant plan add-on) surfaced that the code's own SUPER_ADMIN bypass for that gate is unreachable dead code — every `/org/roles*` route excludes SUPER_ADMIN entirely — documented as a narrow, low-risk candidate, not fixed unprompted. Exercising the newly-granted access then surfaced a real, unrelated HIGH bug in ComplyOS: `GET /certificates` unconditionally overwrote every certificate's status with a pure function of its expiry date, making a revoked certificate — the only lifecycle-exit this record type has — permanently indistinguishable from an active one everywhere, including its own explicit `?status=revoked` filter (which correctly found the revoked rows via the real column, then reported "active" for every one of them). Found and fixed with a one-line change; the ordinary active/expiring/expired path is unaffected. (25) Project OS (deepens the already-open HUD-0032) — attempted to trace portfolios → phases → dashboard → resources → approvals and found every one of six representative endpoints across the module's 34 routes fails identically. Root-caused it directly against the live schema: `project-os.service.ts` references columns on `project_portfolios` and `project_phases` that simply don't exist on the real tables (e.g. `owner_id`/`target_roi` vs. the real `manager_id`/`target_budget`; `sequence_order`/`gate_review_date` vs. the real `phase_number`/`progress_pct`) — comprehensive drift, not one endpoint's bug. Re-rated HUD-0032 HIGH (from LOW) to reflect the true scope now confirmed. Deliberately not fixed: a standing memory note already flags this exact module as another team's concurrent rebuild against a newer migration, with an explicit no-file-overlap boundary — this pass adds fresh live evidence of the drift's true extent, not a fix. (26) FinOps budget-vs-actuals (HUD-0067) — found and fixed a real HIGH bug: voiding any posted bill/invoice touching a budgeted GL account left a phantom variance instead of returning to baseline. Traced live: a real budget line's actuals correctly rose from 1,000,000 to 1,300,000 TZS when a new bill posted, but after voiding that same bill through the platform's own standard void-don't-delete mechanism, actuals read 700,000 instead of the expected 1,000,000 — a live-confirmed **-300,000 phantom**, root-caused to the query's `status != 'VOIDED'` filter excluding the original entry (correct) without also excluding its separate mirror reversal entry (incorrect), breaking the cancellation the reversal exists to provide. Confirmed this wasn't a repeat of a wider pattern by checking `GLService.trialBalance` (the platform's other GL-actuals consumer, which sums every line with no status filter at all and therefore nets voids correctly) and `bank-reconciliation.routes.ts` (uses the same filter, but correctly — there, a reversal is itself a real bank-matchable cash movement). Fixed by removing the filter to match `trialBalance`'s working approach; re-ran the exact original reproduction and confirmed actuals correctly return to 1,000,000 after the void. (27) FinOps GL period close/reopen/lock, MONTH and YEAR, with real corporate-income-tax accrual folded into the year-end close (HUD-0068) — clean. MONTH: a real period closed with no closing entries (just a lock), a further posting inside it was correctly refused platform-wide (the single-chokepoint guard lives in `GLService.post()` itself, not a per-route check), `FINANCE` was correctly refused the reopen (`ADMIN`-tier only) while `TENANT_ADMIN` succeeded with the reason persisted and history preserved, and posting resumed correctly afterward. Delete boundary verified both ways: a never-closed period hard-deletes cleanly; a period that was ever closed (even after being reopened) is permanently refused deletion. YEAR: posted an isolated 1,000,000/400,000 revenue/expense pair in a disjoint test year and hand-verified the entire chain before checking the API — 30% reference-default corporate tax rate (no tenant override, no fixed assets to depreciate) on 600,000 taxable income gives a 180,000 tax liability; confirmed the persisted `cit_returns` row matched exactly, confirmed the tax accrual posts *before* the closing trial-balance snapshot is pulled (so the closing sweep correctly captured the tax expense too, not a pre-tax number), and read the actual closing journal entry line-by-line: Dr Revenue 1,000,000 / Cr Expense 400,000 / Cr Income Tax Expense 180,000 / Cr Retained Earnings 420,000 (true after-tax net income) — balanced exactly. (28) FinOps withholding tax: rate → bill deduction → certificate → remittance (HUD-0069) — clean. Paid a real WHT-tagged bill in two installments specifically to test the "withholds its total WHT once across installments, never per-payment" claim: hand-computed 30,000 then 20,000 (proportional, capped by what's left), matched the API exactly, and the GL postings for both installments (a 3-line Dr AP / Cr Cash-net-of-WHT / Cr WHT-Payable entry each time) balanced to the shilling. Issued a real certificate, confirmed re-issuing it is genuinely idempotent, and read the actual PDF with `pdftotext` rather than trusting the 200 — every figure on it was correct. Batched both deductions into one remittance, confirmed a second batch attempt for the same period correctly finds nothing left, paid it, and hand-verified the GL entry clears the WHT-payable liability back to exactly zero across the whole lifecycle; a repeat payment attempt was correctly refused. (29) Store marketplace-app journey (HUD-0070) — found and fixed a real HIGH authorization gap before even reaching the golden path: `POST /apps` (submit an app for review) had no role check at all, live-confirmed that a `CUSTOMER`-portal account (a tenant's own customer, not a developer) could submit a listing straight into the platform-wide review queue — the one mutating route in the file an earlier pass's `STORE_MGMT_ROLES` fix missed. Fixed with the same CUSTOMER-block convention used everywhere else on the platform; re-verified the exact original reproduction is now refused while a legitimate staff account still succeeds. The golden path itself (submit → admin-only approval queue → approve → public catalog → tenant install/uninstall → reject correctly hides a listing from the catalog) traced clean. Also corrected a real documentation error surfaced by this trace: the platform overview had described "Store" as a "B2B procurement & equipment marketplace," but its entire backend (confirmed by grepping every frontend call site) is a software/integrations marketplace — description corrected as part of this journey. (30) FinOps deferred tax — fixed-asset timing differences, IAS 12 (HUD-0071) — clean. Created a real backdated fixed asset and hand-verified the whole computation before checking the API: a 2-year-old Class 1 (37.5% reducing-balance) asset with zero book depreciation ever posted (a known, already-documented gap — no manual "run now" route exists for the depreciation job) produces a real 9,000,000 TZS temporary difference against the 30% reference rate, landing a 2,700,000 deferred tax liability exactly. Confirmed genuine idempotency (re-running the same date posts nothing new, preserves the same journal entry id) and, more tellingly, confirmed the feature posts only the *incremental movement* between two different dates rather than re-posting the full target balance each time — a second computation a year later correctly posted just the 1,687,500 delta, with the cumulative ledger balance landing exactly on the new target. Self-caught a mistake in my own cleanup-verification script (the exact same VOIDED-status-filter trap as HUD-0067) before it became a false finding. (31) Org chart journey (HUD-0072) — found and fixed a real MEDIUM data-correctness bug: `POST /sync-staff` (import company staff into the org chart) selected every `users` row for the tenant with no role filter, live-confirmed it added two real `CUSTOMER`-portal accounts into the internal staff hierarchy with `job_title: "CUSTOMER"`. Not a security issue (no cross-tenant or unauthorized access), but a tenant's own customers being misclassified as company staff is a real, reachable correctness bug any admin hits by clicking "Sync Staff" on a tenant with active customer-portal accounts. Fixed with the same CUSTOMER-exclusion convention used platform-wide; re-verified live that the two accounts don't reappear on a fresh sync while all real staff still do. The rest of the golden path traced clean: role-gated create/update (`MANAGER`-tier) vs. a narrower delete tier (`ADMIN`+), a `JUNIOR` correctly refused mutation, and — verified live, not just read from the code — deleting a middle node correctly re-parents its children to the deleted node's own parent rather than orphaning them. Reconfirmed (not newly discovered) that org chart remains a standalone visualization feature nothing else on the platform consumes for manager resolution. (32) NexusHR biometric attendance-device journey (HUD-0073) — found and fixed a real HIGH secret-leak bug, with an otherwise genuinely sophisticated pipeline confirmed clean. `GET /attendance-devices` used `.selectAll()`, live-confirmed to leak `push_token` — the sole credential (alongside an equally-readable `serial_number`) authenticating the genuinely unauthenticated `/iclock` device-push endpoint — to every authenticated staff role, including a `JUNIOR` with zero device-management permission, directly contradicting the route's own "shown once, not retrievable again" design comment. Anyone with it could impersonate the physical device and inject fabricated attendance punches for any enrolled employee. Fixed by excluding it from both the list and update responses (confirmed the frontend never reads it from either). The rest of the pipeline traced clean using a route built specifically for this kind of testing (`simulate-punch`, which drives the exact same production code path a real device push uses): a real punch pair correctly produced a real `hr_clock_sessions` row and synced into `hr_attendance` exactly like a web/manual clock-in; an unenrolled PIN correctly landed as an orphan rather than being dropped; assigning an orphan correctly created a new enrollment and reprocessed that historical punch. (33) NexusHR overtime (HUD-0074) — clean, including a live-demonstrated race-condition guard. The statutory rate (1.5x normal, 2.0x rest day/holiday) is correctly derived from the tenant's real calendar, never accepted from the request. Built up real approved claims to exactly the 50-hour rolling four-week cap, hand-verifying the API's own "remaining" figure at every step, then live-demonstrated the exact scenario the code's own header comment names: two claims independently valid at submission time against the same remaining hours, where approving the first correctly makes approving the second fail — proving the cap is re-checked live at approval time, not just at claim time. Hand-verified the payable-amount calculation to the cent across six claims with mixed rates. No code changes needed. **Separately noted, not investigated**: `apps/api`'s typecheck now fails on `email.routes.ts`, a file this session never touched — confirmed via git history that it arrived through two commits made by a different, concurrent session's own in-progress work during this session's Phase 5 pass (that commit also happened to sweep up this session's own then-uncommitted HUD-0067 fix to `budgets.routes.ts`, confirmed still present and correct in the resulting `HEAD`). Left alone as outside this session's scope. (34) Calendar public booking-page journey (HUD-0075) — clean, the first this arc to exercise a genuinely unauthenticated public surface end to end. A real booking page's public info endpoint correctly strips tenant/user identifiers; slot generation matched a hand-computation exactly for a real 09:00–12:00/30-min page; both boundary cases (a non-working weekend day, a date past the booking window) correctly return an empty list rather than an error. The most subtle part — a 15-minute buffer extending a real calendar conflict's blocked window on both sides — was verified exactly: creating one 30-minute blocking event correctly removed three adjacent slots, not just the one it directly overlapped. Booked a slot as a genuinely anonymous caller (no auth at all), confirmed a real calendar event was created with correct guest data and host notification, then confirmed a second attempt on the exact same slot is correctly refused — proving the "re-checked at booking time, not just page-load time" double-booking guard actually holds. No code changes needed. (35) FinOps period-end FX revaluation (HUD-0076) — found and fixed a real HIGH bug. Confirmed the documented "clean baseline" design on a first-ever revaluation (zero movement, nothing posted), then found that a real, pre-existing Draft invoice — which `invoices.routes.ts`'s own code only posts to the GL when its status isn't Draft, so it had zero journal entries — was being revalued anyway, posting a real 5,223.50 TZS phantom gain straight onto the actual Accounts Receivable balance. Checked the AP side and found the identical exposure for bills sitting in DRAFT or PENDING_APPROVAL. Real, everyday impact: any tenant with unfinalized invoices/bills open during a routine month-end FX revaluation gets its real AR/AP balances silently corrupted by gains/losses that were never earned or incurred. Fixed by excluding those exact statuses, matching each document type's own real GL-posting condition. Voided the tainted mixed entry, cleared the contaminated tracking rows, and re-ran the exact same revaluation fresh — the corrected run posted only the two legitimate subjects' correct figures, with no trace of the phantom posting. Also reconfirmed the already-documented same-date idempotency guard still holds. (36) Suppliers journey (HUD-0077) — CRUD traced clean (search-by-contact-name, role-gated create vs. browse-freely list, a real soft-delete that leaves the record as `inactive` rather than removing it). Found a real MEDIUM design-level gap, same shape as HUD-0057's demurrage-liability finding: a supplier's `blocked` status is set and read only within the suppliers module itself — nothing in bill or purchase-order creation ever checks it. Live-confirmed the consequence: marked a test supplier blocked (with a "fraud investigation in progress" note) and then successfully created and posted a real 500,000 TZS bill against it with zero warning or refusal. Not fixed — whether blocking should hard-refuse, warn, or require an override is a product decision. (37) Email journey (HUD-0078) — the first this arc deliberately triggered by a concurrent session's own claim rather than by picking an untraced app, verified independently rather than taken on faith: that session's new memory entry claimed a "100% completion" pass on Email, the same session whose commits briefly regressed this arc's build at journey 33. Confirmed the work is real (three new migrations, a real scheduled-send job on a genuine 15-second scheduler) before tracing it live. Came back clean across everything exercised: a real Gmail-style undo-send (confirmed a deleted still-scheduled message is structurally unreachable to the delivery sweep, not just soft-cancelled), real delivery through the platform's existing mail infrastructure with a real message id, a real read-receipt pixel that stamps a real confirmation timestamp when hit, cross-folder thread reconstruction on reply, genuine Postgres full-text search (a stemmed query matched correctly, not a substring scan), and working custom labels and bulk actions. Self-caught a false alarm along the way — an early "undo" check came back empty only because real wall-clock latency between tool calls meant the message had already been delivered by the real 15-second sweep, not a folder-filter bug. Not exhaustive (IMAP test-connection and quick-reply templates weren't exercised), so not yet re-rated in the overview artifact, but strong positive evidence for the next sync. (38) Notes journey (HUD-0079) — clean across a genuinely sophisticated per-note ACL, optimistic-locking, and legal-hold feature set. A private note is correctly invisible and unreachable to anyone but its creator; a shared note's view-vs-edit permission boundary was verified in both directions (a view-only collaborator refused an edit, then correctly permitted one after an upgrade), while the separate "only the creator manages sharing" rule held even for an editor with full content-edit rights. Optimistic locking correctly refused a stale-timestamp update with real, usable current-state data rather than a bare error, after a collaborator's edit had moved the note forward. Revision history was verified in both directions to lose nothing across two real edits and a restore. Legal hold correctly blocked permanent deletion both individually and through a bulk empty-trash sweep. No code changes needed. (39) Sign forensic case-management journey (HUD-0080) — closing a gap HUD-0045 specifically flagged as untested. Built a real signed-and-completed Sign envelope from scratch, then used the actual public, unauthenticated `/verify/compare` endpoint to submit both the genuine stamped document (a live-computed EXACT_MATCH, real matching SHA-256 hashes) and a deliberately tampered re-creation (a live-computed DOCUMENT_MISMATCH with real structural findings, not a canned response) — confirming the non-clean verdict was auto-opened as a forensic case by the system itself, unprompted, exactly as designed. Found and fixed a real LOW bug: manually opening a case with a note produced two identically-timestamped 'opened' chain-of-custody rows instead of one, because the route fired a second redundant event just to attach the note rather than folding it into the case-opening service's own internal logging. For a system whose entire purpose is an accurate, legally-defensible custody trail, that's a genuine (if minor) correctness defect; fixed by threading the note through the same call, re-verified live to produce exactly one row. Confirmed re-analysis versioning never overwrites a prior run, report generation correctly appends to the evidence manifest, and every view/export is genuinely logged as its own custody event. Thirty-nine journeys of many — several other apps' core journeys remain untraced |
| Data Integrity & DB | 10% | 70% | RLS solid; migration drift now fixed+proven (HUD-0009); constraints/precision/concurrency unaudited |
| API & Integrations | 10% | 45% | 195 routes not contract-checked; 9 integrations unaudited |
| Auth / RBAC / Tenancy | 10% | 70% | tenancy PASS; auth spot PASS; **HUD-0024's route-by-route RBAC sweep is now CLOSED — all 196 route files read and triaged at least once, 105 gaps found+fixed live (3 CRITICAL), 91 files confirmed already correctly built.** Highlights: HUD-0036 (38 files, the largest batch) found nearly all of Finance/Fleet/HR/Ondi-identity-admin had only a plan-entitlement gate, not a role gate; HUD-0037 found 4 more SEAL-family siblings the original HUD-0031 sweep missed (the "shared URL prefix ≠ shared guard" trap recurring a third time) plus a full-CRUD gap on delivery documents and a quotations IDOR; HUD-0038/0039 closed the remainder, the last ~45 files mostly already correct (the CRM family, onsite-agency and superadmin families, a carefully OTP-gated raw-SQL tool). **Closing this sweep is not the same as proving RBAC is airtight** — the method asked one repeatable question (role gate present or absent) of every file once; it cannot see a role check that's present but wrong, or a flaw spanning multiple routes/services/the RLS layer. Phase 17 (independent adversarial pass) and Phase 5 (functional tracing) remain the next real check on that, and Phase 5 already found one: HUD-0058 (Phase 5's sixteenth journey) found `quotations.routes.ts`'s `PATCH /:id/status` — a *different* route in the same file HUD-0037 already fixed a quotations IDOR in — had no role check at all, live-confirmed as a real cross-customer bypass (a `CUSTOMER` JWT correctly refused a `GET` on another customer's quote could nonetheless silently approve/reject it). Fixed with the same `QUOTE_WRITE_ROLES` guard its sibling routes already use. Proof that "this file has a role gate" (HUD-0024's per-file question) does not mean every mutating route in it does — exactly the residual risk this row already named. Phase 5's twenty-ninth journey found a second, milder instance of the same shape: HUD-0070 found `store.routes.ts`'s `POST /apps` — in the same file whose `STORE_MGMT_ROLES` guard already fixed the install/uninstall routes — had no role check at all, live-confirmed that a `CUSTOMER`-portal account could submit a listing into the platform's marketplace review queue. Fixed with the same CUSTOMER-block convention used everywhere else on the platform |
| Reliability / Error handling | 8% | 55% | HUD-0021 (process-crashing pool bug) found+fixed; broader error-handling sweep not done |
| Testing & Regression | 8% | 20% | HUD-0005 open, but CI (HUD-0006) now runs the tests that do exist on every push |
| Performance | 7% | 40% | not measured |
| Responsive / Mobile | 5% | 40% | not tested |
| UI / UX | 5% | 55% | design-system exists + enforced by CLAUDE.md; not forensically audited |
| Accessibility | 3% | 35% | not audited |
| DevOps / CI/CD / Observability | 4% | 65% | HUD-0006 fixed (real CI); HUD-0022 (lint) still open |
| **Overall** | 100% | **≈ 55%** | |

### Mandatory gates

| Gate | State |
|------|-------|
| CRITICAL security = 0 | ✅ PASS — one found+fixed live this session (HUD-0058, a cross-customer quotation-approval bypass), zero known open |
| CRITICAL data = 0 | ✅ PASS |
| CRITICAL functional = 0 | ◑ PARTIAL — Phase 5 has now traced 16 golden paths live (stale "not run" note below corrected); zero CRITICAL-functional (crash/data-loss) findings among them, but most of the platform's core journeys remain untraced |
| HIGH security/tenancy/authz = 0 | ❌ 4 open (HUD-0004/0005/0007/0008) — HUD-0024 (RBAC route sweep) is now CLOSED, corrected from an earlier stale count in this row |
| Tenant isolation | ✅ PASS (schema layer, verified) |
| Authentication | ◑ PASS (spot) |
| Authorization | ◑ **PARTIAL** — HUD-0024's route-by-route sweep is CLOSED: all 196 route files triaged at least once, 105 gaps found+fixed live (3 CRITICAL), 91 confirmed already correct, zero known open from that method. Still PARTIAL, not PASS: the sweep proves "role gate present or absent" once per file, not that a present check is *correct*, nor that no flaw spans multiple routes/services — that's what Phase 5 (functional tracing, in progress) and Phase 17 (adversarial pass, not started) are for |
| Core workflows E2E | ◑ PARTIAL — 16 golden paths traced live with real DB verification (Phase 5); most apps' core journeys still untraced |
| Production build | ✅ **PASS** — was broken (HUD-0020), fixed and verified |
| DB migrations reproduce prod | ✅ **PASS** — proven on a from-scratch database (HUD-0009) |
| CI meaningful | ✅ **PASS** — real gate added (HUD-0006) |
| API resilient to a dropped DB connection | ✅ **PASS** — was FAIL, crashed live during this audit (HUD-0021) |
| No production mocks | ❌ UNVERIFIED |
| No exposed secrets (client) | ✅ PASS |

## 5. Recommended next order

1. ~~HUD-0009~~ — **done.** Migration history drift fixed and proven against a
   from-scratch database (0-diff vs. live).
2. ~~HUD-0005 test #1~~ — **done.** Permanent tenant-isolation regression test
   (`tenant-rls-coverage.test.ts`, 7 assertions) added and passing.
3. ~~HUD-0006~~ — **done.** Real CI (`ci.yml`): typecheck, fresh-DB migrate, API tests
   against that fresh DB, full build. Proven locally end to end before being trusted.
4. ~~HUD-0020~~ — **done.** `npm run build` was broken (`packages/ui` couldn't compile);
   fixed, all 4 workspaces build clean.
5. ~~HUD-0021~~ — **done.** Found live mid-audit: an unhandled `pg` pool error crashed
   the whole API on any dropped DB connection. Fixed on all 4 connection pools.
6. ~~HUD-0023~~ — **done, partial Phase 14.** Automated triage of all 195 route files
   found ~300 mutating-endpoint candidates with no role/ownership check across 58 files;
   the highest-stakes one (Onsite — servers/domains/deploy secrets) was read end to end
   and proven live: a `CUSTOMER`-role JWT could create and delete infrastructure. Fixed,
   re-verified live (403 for low-privilege roles, 200 unchanged for admin roles).
7. ~~HUD-0025 / HUD-0026~~ — **done.** ComplyOS Legal Marketplace (create/cancel legal
   engagements, message a law firm, flip milestone payment status) and the shared
   identity-avatar endpoint (any user could overwrite anyone else's photo or a company's
   logo, platform-wide) both had the same shape of gap. Both fixed, both proven live —
   including confirming self-service avatar editing (the real product feature) still works.
   `billing.routes.ts` was checked and found already correctly gated;
   `seal-billing.routes.ts` and `org.routes.ts POST /claim` were checked and are
   deliberately open by design (draft-only invoice, one-time-code capability token).
8. ~~HUD-0027 / 0028 / 0029 / 0030~~ — **done.** Sign (ownership, not role — void/resend/
   retitle/edit any other user's envelope), Contacts and CMS/OneSite (both fully reachable
   by a `CUSTOMER`-role portal account), and Onsite Backups (the exact HUD-0023 gap,
   missed because it's a separate route file) all fixed and proven live. Discovered that
   "does a `CUSTOMER` JWT even reach this app's base route" is a fast, high-confidence
   signal — 4 of 4 apps checked that way turned out to be real bugs — and used it to probe
   the remaining flagged files quickly.
9. ~~HUD-0031~~ — **done.** That probe's 11 flagged candidates turned into 21 confirmed
   gaps once each file was actually read (several apps had sibling plugin files — seal-*
   (8), inventory-* (3), hr/nexushr, declarations/declaration-ledger-anchor — carrying the
   identical gap independently). All fixed with the same minimal, additive per-file hook;
   `org.routes.ts` got the stricter `role === 'ORG'` check instead, since every route in it
   assumed that token shape with no runtime check. Re-verified live in one batch: every
   endpoint now 403s for `CUSTOMER`, unaffected for legitimate internal roles. Found one
   non-auth bug as a side effect — Project OS's `GET /portfolios` 500s for any real user,
   not just `CUSTOMER` — logged as HUD-0032 and left alone (active concurrent development).
10. ~~HUD-0033~~ — **done, second pass after a session boundary.** Re-verified HUD-0001–0031
    all survived intact (an automated commit had captured everything) before continuing. Found
    the audit's most serious result: `sign-stamps.routes.ts`'s `PUT`/`DELETE /stamps/tenant`
    (the tenant's one official e-signature stamp) had zero check — a real forgery vector, and
    the file already had the right role constant declared but never wired to these two routes.
    `sign-versions.routes.ts` turned out to be a whole sibling file of `sign.routes.ts` with no
    entitlement or ownership check at all, missed by HUD-0027 — fixed by exporting and reusing
    that file's own ownership check rather than duplicating it. Six more apps (Customs, AI
    memory, CargoTracker, HuduBI, HuduFreight cargo-loading/warehouse) were CUSTOMER-reachable
    and fixed the same way as HUD-0031. Six files checked and confirmed already correct,
    including `notifications.routes.ts`'s existing `customer_id` branch for `role === 'CUSTOMER'`
    — proof this pass isn't rubber-stamping fixes onto files that don't need them.
11. ~~HUD-0024 remainder~~ — **done (HUD-0035–0039).** All 196 route files now triaged at
    least once; 105 files fixed (3 CRITICAL), 91 confirmed already correct. See HUD-0024's own
    entry for the closure note on what this method does and does not prove.
12. ~~Phase 6 remainder~~ — **done (HUD-0040/0041/0042).** SSRF (4 fixed), log-secret scan
    (clean), path traversal/file access (1 fixed), SQL/command injection (clean). Webhook-
    receiver signature verification on the SMS delivery-status callbacks was found and
    documented but not fixed (needs a live Twilio sandbox to test against safely).
13. **Phase 5 (next)** — functional tracing of the core journeys per app (mandatory gate:
    Core workflows).
13. **HUD-0004 / HUD-0008** — finish non-tenant table classification.
14. **HUD-0022** — wire up real linting (currently completely non-functional).
15. **HUD-0019** — decide the fate of the two dead marketplace-request tables.
16. Phases 7–12 (data integrity, UI/UX, responsive, a11y, performance, integrations).
17. Phase 17 — independent adversarial pass.

## 6. Risks carried

- Concurrent development is active on this branch (multiple `apps/api` files modified
  outside this audit's scope during the session). Re-run the RLS scan after any
  migration lands to catch a new `tenant_id` table shipped without a policy.
- The `text`→`uuid` `tenant_id` migration (HUD-0007) touches live compliance data and
  needs a deliberate, tested rollout.
- No load/volume testing has been done; all evidence is against a dev dataset
  (largest table `api_usage_events` ≈ 666k rows).
