# Hudumika — Production Readiness Report (in progress)

**Status: PHASE 1–4 COMPLETE. PHASE 9/35/36 (migrations, build, reliability)
COMPLETE. PHASE 14 (RBAC route sweep) CLOSED — all 196 route files triaged,
105 gaps found+fixed, zero known open (HUD-0024/0039). PHASE 6 (full security
sweep) now also CLOSED — SSRF, path traversal/file access, and SQL/command
injection all checked with live evidence, 5 more real gaps found+fixed
(HUD-0040/0041/0042); one webhook-signature gap found and documented, not
fixed. NOT YET 98% — Phase 5 (functional tracing) is next.**
Date 2026-09-13. This is a live document; it will be wrong the moment the next
phase runs. Do not cite the score below as final.

---

## 1. Executive summary

The audit began at Phase 1 (discovery) and ran through Phase 4 (auth / RBAC /
tenant isolation), a partial Phase 6 (transport security), and the deployment/CI
phases (35/36). Four CRITICAL and three more HIGH-severity production blockers
were found — each with live evidence, not inferred — and fixed:

> **81 tenant-scoped tables and 9 line-item tables had no Row-Level Security.**
> The restricted application database role could read and write every tenant's
> email, HR, compliance, shipment, API-usage and settings data by setting its
> tenant variable to any value. Proven live against the running database.

> **A fresh install could not reproduce production's schema**, and would have
> crashed partway through: one seed migration assumed a user already existed,
> and one table referenced by this audit's own RLS fix had no creating migration
> anywhere in the repo. Proven by actually running all 470 migrations against a
> database that had never seen data — not assumed from the file-count mismatch.

> **`npm run build` had likely never succeeded** — a shared package couldn't
> compile under its own tsconfig. **CI enforced none of this** — the only
> workflow ran `npm audit`, non-blocking.

> **Any dropped database connection crashed the entire API process** — for
> every tenant at once, with no reconnect. This happened live, mid-audit,
> confirming the finding rather than requiring it to be theorized.

> **A customer-portal account could create and delete infrastructure.**
> `onsite.routes.ts` (servers, domains, deployments, deploy secrets — ~50
> mutating endpoints) checked only whether the tenant's plan included the
> feature, never which role the caller had. Proven live: a real `CUSTOMER`-role
> JWT created and deleted a real database row through the actual route.

All five are fixed and re-verified (fresh-database dry run passes end to end;
full API suite green against both the live and a from-scratch database; the
build succeeds; the crash-causing gap is closed on all four connection pools;
the same customer JWT that could mutate infrastructure now gets 403 on a plain
read, while a legitimate admin role is unaffected). A real CI workflow
(`ci.yml`) now runs the build/test/migration chain on every push/PR.

Phase 14 (the RBAC matrix) is now a multi-session effort in progress: an
automated triage of all 195 route files for mutating endpoints with no role
or ownership check flagged ~300 candidates across 58 files; most inspected so
far turned out to be legitimate patterns (named-approver checks, alternate
guard function names, deliberate `410 Gone` stubs, a deliberate draft-only
invoice path, a one-time-code capability-token flow) rather than real gaps.
**Eight were real** and are now fixed across 7 files, each proven live with a
real low-privilege JWT, not inferred from reading code alone:

- **Onsite + Onsite Backups** (HUD-0023/0030) — a `CUSTOMER`-role JWT created
  and deleted infrastructure records and could restore/delete config backups;
  the fix to one file didn't cover the other, which used the identical gap.
- **ComplyOS Legal Marketplace** (HUD-0025) — any tenant member could
  create/cancel legal engagements and flip milestone payment status.
- **Identity avatars** (HUD-0026) — any tenant member could overwrite or
  delete anyone else's profile photo or a customer/lead/driver/supplier's
  logo, platform-wide (the shared `PersonAvatar`/`CompanyAvatar` endpoint).
- **Sign envelopes/templates** (HUD-0027) — no mutation route checked who
  created the envelope; any tenant member with 'sign' entitlement could void,
  resend, retitle or fully re-edit someone else's document, including a
  still-private draft — contradicting a creator-only rule the list endpoint
  already enforced elsewhere in the same file.
- **Contacts + CMS/OneSite** (HUD-0028/0029) — both reachable in full by a
  `CUSTOMER`-role portal account: the internal address book, and the
  tenant's own public website (edit/delete pages, overwrite site settings).

A systematic "can a `CUSTOMER` JWT even reach this app" probe against the
remaining flagged files (once that pattern proved out 4 different times)
turned up **21 more real gaps across 23 files — all now fixed and proven
live** (HUD-0031): Petti wallets, Sanctions screenings, Notes, SEAL
compartments (+ 8 sibling `seal-*` files, each its own plugin registration —
the same one-fix-doesn't-cover-the-sibling-file lesson HUD-0023/0030 already
taught), HR departments (+ `nexushr.routes.ts`, a separate file on the same
`/v1/hr` prefix), Depot equipment, Inventory warehouses (+ 3 sibling files),
Dangerous Goods declarations, and Declarations (+ its ledger-anchor sibling)
were all reachable in full by a `CUSTOMER`-role JWT. `Org tickets` was a
distinct sub-case — every route in `org.routes.ts` cast the JWT straight to
an `OrgJWTPayload` shape with no runtime check it actually *was* one; fixed
by requiring `role === 'ORG'` outright rather than just excluding `CUSTOMER`.
Project OS's `GET /portfolios` also threw a **500 for a legitimate `JUNIOR`
JWT**, proving that crash was a pre-existing, role-unrelated bug, not
something the auth fix caused or could paper over — logged separately as
HUD-0032 and left for the team actively developing that module.

Two apps were checked and found already correct, untouched: Shipments (real
`customer_id`-scoped filtering with a safe non-matching fallback) and Drives
(already blocked for `CUSTOMER` by a concurrent session's own fix).
`/v1/reference/*` was checked and is deliberately open, global reference
data (tariffs/excise/ICD operators) — correct as-is.

A second pass (HUD-0033, after a session boundary — every prior fix was first
re-verified intact and committed) found **10 more real fixes**, including the
most serious finding of this whole audit: **`sign-stamps.routes.ts`'s
`PUT`/`DELETE /stamps/tenant` had no check at all on the tenant's one official
e-signature stamp image** — any authenticated user with 'sign' entitlement,
including a `CUSTOMER`, could overwrite the company's legal stamp with an
arbitrary image (a real forgery vector) or delete it. The file already had a
correctly-gated sibling endpoint and even the right role constant declared —
it was just never wired to the two routes that touch the actual image.
`sign-versions.routes.ts` (envelope version history/restore) turned out to be
an entire sibling file of `sign.routes.ts` with **no entitlement check and no
ownership check at all**, missed when HUD-0027 fixed the main file — fixed by
exporting and reusing that file's own ownership-check function rather than
duplicating it. Six more apps (Customs, AI memory, CargoTracker, HuduBI,
HuduFreight's cargo-loading and warehouse route files) were fully reachable
by `CUSTOMER` and fixed the same way as HUD-0031. Six files were checked and
confirmed already correct — most notably `notifications.routes.ts`, which
already explicitly branches on `role === 'CUSTOMER'` to scope by
`customer_id` instead of `user_id`, proving not every file needed fixing and
this audit isn't just pattern-matching blind.

**~30 files remain fully untriaged**, and one real methodological gap is now
explicit: every fix in HUD-0023–0033 came from a scan for *mutating* routes
with no role check — a file with only unscoped GET routes was never flagged
by that scan at all and would need a separate, broader sweep to catch. This
remains a partial result, not a completed authorization audit, even though
every gap actually found has been fixed.

**The remaining 44 sections of the master audit have not been executed.** Large
areas — functional tracing of every action to the database, the full RBAC matrix,
data-integrity/concurrency, UI/UX, responsive, accessibility, performance at
realistic volume, the 9 third-party integrations, and test authoring — are
UNVERIFIED and are explicitly *not* marked PASS.

## 2. What was done

| Phase | Area | Result |
|------:|------|--------|
| 1 | Repo / architecture discovery | ✅ `ARCHITECTURE_MAP.md` |
| 2–3 | App / route / table inventory | ✅ (195 routes, 153 services, 40 jobs, 580 tables, 470 migrations, ~35 web shells, 468 pages) |
| 4 | Multi-tenant isolation | ✅ **3 CRITICAL fixed + verified** (HUD-0001/0002/0003); residual triaged (HUD-0004/0008) |
| 4 | Auth / session | ◑ spot-checked PASS; full flow pass pending |
| 6 | Transport security | ◑ CORS / helmet / rate-limit / client-secret scan PASS; SSRF / path-traversal / IDOR / file-upload / webhook-sig / log-scan pending |
| 14 | CI/CD | ✅ audited → **FAIL** (HUD-0006) |
| 13 | Test coverage | ✅ audited → **FAIL** (HUD-0005) |

## 3. Issues

| ID | Sev | Title | Status |
|----|-----|-------|--------|
| HUD-0001 | CRITICAL | 79 tenant tables no RLS | FIXED (VERIFIED) |
| HUD-0002 | CRITICAL | `shipment_cases` partitions unprotected | FIXED (VERIFIED) |
| HUD-0003 | CRITICAL | 9 child/line-item tables no RLS | FIXED (VERIFIED) |
| HUD-0021 | CRITICAL | Any dropped DB connection crashed the whole API | **FIXED (VERIFIED)** — found live during this audit |
| HUD-0023 | CRITICAL | Onsite: any authenticated role (incl. CUSTOMER) could create/delete infrastructure | **FIXED (VERIFIED)** — proven live with a real CUSTOMER JWT |
| HUD-0009 | HIGH | Migration history drift — fresh DB didn't reproduce prod | FIXED (VERIFIED) |
| HUD-0006 | HIGH | CI had no build/typecheck/test/migration gate | **FIXED (VERIFIED)** |
| HUD-0020 | HIGH | `npm run build` was broken (`packages/ui`) | **FIXED (VERIFIED)** |
| HUD-0025 | HIGH | ComplyOS Legal Marketplace had no role gate | **FIXED (VERIFIED)** |
| HUD-0026 | HIGH | Any user could overwrite/delete anyone else's avatar or a company logo | **FIXED (VERIFIED)** |
| HUD-0030 | CRITICAL | Onsite Backups: same gap as HUD-0023, separate file, missed by that fix | **FIXED (VERIFIED)** |
| HUD-0027 | HIGH | Sign: no envelope/template mutation checked who owned it | **FIXED (VERIFIED)** |
| HUD-0028 | HIGH | Contacts: internal address book reachable by CUSTOMER accounts | **FIXED (VERIFIED)** |
| HUD-0029 | HIGH | CMS/OneSite: CUSTOMER account could edit/delete the tenant's public website | **FIXED (VERIFIED)** |
| HUD-0004 | HIGH | ~25 non-tenant tables unclassified for isolation | OPEN |
| HUD-0005 | HIGH | ~no automated test coverage (10 API / 0 web) | OPEN |
| HUD-0007 | HIGH | `tenant_id` type split uuid/text (12 tables) | OPEN |
| HUD-0008 | HIGH | Developer/analytics tables isolation unconfirmed | OPEN |
| HUD-0031 | HIGH | 21 more CUSTOMER-reachable internal endpoints across 23 files | **FIXED (VERIFIED)** |
| HUD-0033 | CRITICAL | Sign stamp forgery gap + Sign-versions ownership gap + 6 more CUSTOMER-reachable apps | **FIXED (VERIFIED)** |
| HUD-0024 | HIGH | RBAC matrix triaged, not completed — ~30 route files still fully unreviewed | OPEN |
| HUD-0010..0018 | MED/LOW | Repo hygiene, empty catch, env defaults, `tenant_settings` sentinel row | OPEN |
| HUD-0019 | LOW | Two dead, orphaned "marketplace request" tables (one now RLS-protected regardless) | OPEN |
| HUD-0022 | MEDIUM | `npm run lint` completely non-functional — eslint not installed, no config | OPEN |
| HUD-0032 | HIGH (re-rated from LOW — Phase 5 confirmed the whole module, not one endpoint) | Project OS: comprehensive schema drift between `project-os.service.ts` and the live database — every one of 6 representative endpoints spot-checked across the module's 34 routes (portfolios list/create, phase create, dashboard, resources, approvals) fails identically; root-caused to columns referenced in code that don't exist on the real tables | OPEN — left for the team actively developing that module (standing "no file overlap" boundary, [[project_os_transformation_role_split]]) |

## 4. Production readiness score (provisional)

Weighted per the master model. A dimension is scored only on what has been
*verified*; unverified dimensions are capped at 50% ("cannot confirm").

| Dimension | Weight | Score | Notes |
|-----------|------:|------:|-------|
| Security & Privacy | 15% | 76% | RLS gate PASS at schema layer; auth spot-checked. **Phase 6 (full security sweep) is now CLOSED**: SSRF (HUD-0040, 4 real gaps found+fixed live — Onsite uptime monitor, workflow webhook, GPSWOX base_url, marketplace webhook dispatch, via one shared `assertPublicHttpUrl` guard); log-secret scan (clean — default Fastify serializer never logs headers/body); path traversal / file access (HUD-0041, 1 real gap found+fixed live — `POST /v1/tra/register` took a client-supplied filesystem path with zero validation, fixed by having it look up the tenant's own uploaded cert directly; the Drive/file-storage layer was independently reviewed and found already correctly double-defended); SQL/command injection (HUD-0042, clean — the codebase's pervasive use of Kysely's parameterized query builder, plus a single safe `execFile`-with-argv-array command-execution site, leaves no real injection surface). Found but not fixed: SMS delivery-status webhooks (`africas-talking`/`twilio`) accept an unsigned callback — low exploitability, documented rather than guessed at since an untested Twilio-signature implementation could silently break real delivery tracking. Dep-vuln backlog still unaudited (outside Phase 6's named scope) |
| Core Functionality | 15% | 85% | **Phase 5, eighty-six journeys traced live.** (1) ClearOS shipment → workflow engine (correctly *rejecting* two invalid transitions) → FinOps invoice → GL → payment (HUD-0043) — found and fixed a real bug: the invoice line-item schema documented fields the insert code never read, crashing any caller who trusted it. (2) NexusHR payroll run → GL (HUD-0044) — clean, retired a stale "payroll→GL not wired" note; posted journal balanced, PAYE figure matched Tanzania's statutory formula exactly. (3) Sign envelope create→send→sign→stamp→verify (HUD-0045) — clean; complete audit trail, working public verification. (4) SEAL bonded-warehouse lot → customs declaration → duty computation → release (HUD-0046) — clean; the CIF/duty/RDL/CPF/VAT math was independently recomputed by hand and matched the API's own figures to the shilling, and the tamper-evident movement chain verified. (5) Drive upload → download → public share link → access-log (HUD-0047) — clean; the file's bytes survived storage byte-for-byte through both an authenticated download and a fully anonymous public-link download, and the access log correctly distinguished the two with accurate attribution on each. (6) CRM lead → convert → deal → pipeline stage progression → Won (HUD-0048) — clean; converting a lead left the lead's own stage genuinely untouched (verified against the row, not just the response), the timeline was fully and correctly attributed across both entities, and `closed_at` was stamped only on reaching the tenant's real configured `is_won` stage, not a hardcoded string match. Four of six record types (invoices, payroll, sign, SEAL) share one confirmed design — no hard delete once posted, only advance/reverse through a real state machine; Drive's delete story is looser (permanent delete needs a platform-superadmin credential); CRM leads/deals were the first traced type with a real, unrestricted hard-delete, and both test records were cleanly removed that way. (7) Support ticket create → staff reply → customer reply → resolve (HUD-0049) — mostly clean: first-reply and resolution SLA timers matched hand-computed diffs exactly, the ticket auto-transitioned OPEN→IN_PROGRESS on first reply, and a non-owning customer was correctly refused with a 404. One LOW gap noted, not fixed: the ticket list/detail GET endpoints omit the SLA timer fields from their SELECT lists (confirmed live), even though the write response and the separate aggregate dashboard both see them correctly — a latent gap for a future per-ticket SLA badge, not a currently broken feature (grepped the whole frontend, zero consumers read these fields today). (8) Onsite domain → DNS zone/records → server provisioning (HUD-0050) — real bug found and fixed: `POST /servers` crashed with a bare 500 for any non-lowercase provider name ("DigitalOcean" instead of "digitalocean") because the DB's case-sensitive CHECK constraint had no matching validation in the API schema; the shipped UI's own controlled dropdown avoids this today, but any other caller wouldn't have. Fixed with a case-normalizing enum validator; re-verified the exact original crashing payload now succeeds and a genuinely invalid provider gets a clean 400. The DNS safety mechanics (invalid-record rejection, confirm-before-deleting-the-apex-record, SSRF-safe propagation checking) all traced correctly. (9) NexusHR recruitment: requisition → submit → approve → publish (real `hr_job_openings` row) → candidate/application (with working per-opening dedup) → interview (creating a real linked `calendar_events` row, verified directly in Postgres) → offer → submit → approve → send → accept (HUD-0051) — clean, and more tightly wired than expected: sending an offer generates a genuine PDF and reuses the platform's own Sign app (`sign_envelopes`/`sign_recipients`) to give the login-less candidate a real signable link, and accepting one stamps the application `HIRED` and creates a real `hr_invitations` row (not a fabricated flag) with accurate domain events and activity-log entries end to end. Every invalid-transition and duplicate-application guard tested returned the correct 400/409. (10) Petti petty-cash wallet: create wallet (real auto-provisioned `PW-####` GL account) → deposit → named-approver setup → withdrawal request, with a live adversarial-shaped test of every authorization boundary — requester blocked from approving their own request, a `FINANCE`-role non-approver blocked from approving (proving the named-approver check outranks the coarse role check), the real approver's `disburse` attempt blocked (approving ≠ disbursing), then a correct `FINANCE` disburse producing a real balanced GL entry and a `finance_expenses` cash-advance row — plus an oversized second request correctly refused at disburse against the live derived balance (not a stale snapshot), and a self-service backup-approver who successfully rejected a third request with no finance-admin involvement (HUD-0052) — clean; the downloaded voucher PDF was independently read with `pdftotext` and every figure/name on it was real, not a placeholder. One LOW gap noted, not fixed: an `approved` withdrawal request has no `cancel`/`void` path if it turns out to be undisbursable — it sits in permanent limbo rather than blocking anything. (11) Studio automation builder: created a real trigger→condition→action automation and fired it not through either manual test endpoint but by creating a real CRM lead through the real `POST /v1/leads` endpoint, proving the actual production path — the real domain-event bus wired to `subscribers/studio.subscribers.ts` — actually runs a tenant's automations with zero manual intervention (HUD-0053). Found and fixed a real HIGH bug: the `tasks.create_task` action's `INSERT INTO tasks` omitted `id`, which has no DB default and is `NOT NULL` — a 100%-reproducible crash on every real invocation, invisible via the builder's "Run Test" button because the dry-run path short-circuits before ever reaching the insert. Every other insert site into `tasks` across the codebase already supplies a generated id; this was the one exception. Fixed by adding `id: crypto.randomUUID()` to match; re-verified live (a fresh high-value lead now correctly creates a real, template-resolved task; a low-value lead correctly evaluates the condition false and skips the action with zero side effects; the dry-run endpoint was re-confirmed to write nothing to the database, explaining exactly how the bug shipped unnoticed). (12) Inventory Control: warehouse → item → stock receipt → issue → physical count session with a real 5-unit shrinkage, checking GL account `1300` (Inventory) at every step (HUD-0054) — found a real MEDIUM-HIGH gap, documented rather than fixed: receiving 500,000 TZS of stock posted nothing to the GL, issuing 30 units correctly credited `1300` for its COGS (confirming the wiring that *does* exist works), and then physically confirming a 25,000 TZS stock loss via a real count-session post *also* posted nothing — the account's balance was byte-for-byte identical before and after the shrinkage. Grepped account `1300` platform-wide: it is credited only by `issue` and debited nowhere at all, so ordinary Inventory-app usage can only ever push it further from reality. A second disconnect confirmed by code: Purchase Orders never call into Inventory's stock ledger at all — marking a PO “Received” only changes a status label. Not fixed on the spot because a real fix needs product decisions this trace can't make (which clearing/write-off GL accounts to introduce, whether an approval step belongs in front of them, mirroring the workflow Petti already has). **Closed as a same-day, user-directed follow-up (HUD-0135)**: introduced two new accounts (`2050` Goods Received Not Invoiced, a clearing liability for stock that's arrived before its bill has; `5011` Inventory Shrinkage/Write-off, one account for both directions of a physical count correction), backfilled onto all 9 existing tenants and added to the standard chart of accounts for new ones, then wired `receipt` and `adjust`/`count_correction` to post against them — `issue`'s pre-existing COGS posting was already correct and untouched. The Purchase-Orders-never-call-Inventory disconnect is deliberately still open; that's a separate, larger product decision (should a PO receipt auto-create a movement, and does that need a match-to-bill step) this fix didn't make unilaterally. (13) ComplyOS renewals: certificate → start renewal → approve → update the certificate's own expiry date (the real action a compliance officer takes once a renewal is actually granted) (HUD-0055) — two findings. Found and fixed a real HIGH bug: `GET /renewals` failed unconditionally for every tenant regardless of data — `comply_renewals.approved_by` is `text`, `users.id` is `uuid`, and the join between them had no cast, which Postgres rejects at plan time (the 13th confirmed instance of the same uuid/text-mismatch pattern tracked platform-wide as HUD-0007). Fixed with an explicit `::uuid` cast in the join, matching an existing convention elsewhere in the codebase; re-verified live. Found a real MEDIUM workflow dead-end: nothing in the codebase ever advanced a renewal past `approved` to `submitted`/`completed` — live-verified that after a certificate was genuinely renewed (its expiry pushed a year out), starting the *next* renewal cycle was still refused as "already active," permanently, because the first approved renewal could never be cleared, guaranteeing every certificate hits this on its second cycle. Fixed on explicit follow-up: `updateCertificate` now completes any in-flight renewal for a certificate whenever its `expiry_date` changes, reusing the exact signal the codebase already treats as "a real renewal happened" rather than adding a new endpoint. Re-ran the original failing reproduction end to end — the second renewal cycle now starts cleanly (`201`) instead of being refused. (14) CargoTracker freight booking: carrier → buy-side rate contract → rate-shopping (correctly surfaced and sorted) → booking → quote → confirm (HUD-0056) — clean; confirming a quoted booking genuinely converts it into a real, correctly-fielded `shipment_cases` row (right type from the booking's mode, right starting stage, vessel/BL/route carried across) rather than just flipping a status, and the reverse `by-shipment` lookup `ShipmentDetail.tsx` depends on resolves correctly. Every state-machine guard tested (re-quoting a quoted booking, cancelling a confirmed one, cancelling one still requested) behaved exactly as coded. (15) Demurrage → GL → invoice recharge: tariff → container with a real discharge date → tiered demurrage cost (hand-verified exactly) → posted to the GL (dry-run had zero side effects, the real post created a balanced entry, idempotency correctly skipped a repeat) → recharged onto a real customer invoice as a real line item, with a double-recharge correctly refused (HUD-0057) — the cost pipeline itself is clean. Found a real MEDIUM-HIGH gap, documented, not fixed: the schema and GL logic both fully support recording a demurrage charge as the company's own fault (absorbed as an expense, never billed to the customer) — a dedicated column, a CHECK constraint, and a fully-built posting branch all exist for it — but nothing in the entire codebase can ever set it. Live-verified the silent failure directly: `PATCH` a container with `liable_party: "COMPANY"` returns a plain `200` and changes nothing, with no error to notice by. Every demurrage charge on this platform is therefore permanently billed to the customer regardless of whose fault the delay actually was. Scoped narrowly enough (unlike HUD-0054) that a future session could close it in one targeted change if asked. **Closed as a same-day, user-directed follow-up (HUD-0132)**: `liable_party`/`liability_reason`/`status` are now writable through the same PATCH endpoint, each validated against their real allowed values, plus a new guard that refuses the change once the charge has actually been posted to the GL (changing who's liable after the fact would desync the container from what was really debited). Re-ran the exact original reproduction end to end — the `PATCH` that used to silently do nothing now genuinely persists all three fields, and correctly refuses once posted. (16) Quotations: create → attempt approve as a *different* customer's authenticated JWT → convert to shipment (HUD-0058) — found and fixed two real bugs. A CRITICAL/HIGH authorization bypass: `PATCH /:id/status` had no role check at all, live-confirmed that a customer correctly blocked from even viewing another customer's quote could nonetheless silently approve or reject it — fixed with the same role guard the file's own sibling routes already use. A HIGH attribution bug: three call sites in the same file passed a nonexistent `user.id` (the JWT payload only has `.sub`) hidden behind an `as any` cast, so `prepared_by`, `approved_by`, and — most consequentially — the `assigned_to` of every shipment created by converting a quote were silently `null` for every real caller; fixed by using `.sub`, re-verified directly against Postgres. (17) Fleet dispatch → border crossing → trip billing: vehicle → driver → trip → dispatch, with a live double-booking check (a second trip on the same vehicle/driver succeeds while the first is only planned, then is correctly refused once the first is actually dispatched) and a live non-dispatchable-vehicle check (a vehicle marked `MAINTENANCE` is refused even at trip *creation*) → a real border-crossing event correctly cleared → a billable trip expense correctly bridged into a real Draft invoice, with a second billing attempt on the same trip correctly refused (HUD-0059) — clean; the platform's own "operational cost invisible to finance" pattern (the same shape of gap HUD-0057 found and fixed for demurrage) was already correctly built here. (18) Supplier bills → GL → payment → void (HUD-0060) — found and fixed a real HIGH bug, same family as HUD-0043 but a worse failure mode: the bill line schema documented fields (quantity/unit_cost/amount) the real line-processing code has never read (it reads qty/unit_price/category, matching what the real frontend sends) — but unlike HUD-0043's crash, a caller trusting the documented schema here would get a bill that's created successfully with every line silently priced at zero, no error at all. Fixed to match reality. Everything else in this file is clean and genuinely sophisticated: tax-recoverability splitting hand-verified to the shilling across a recoverable and a non-recoverable line, category-based GL account routing, a two-installment payment correctly posting its own Dr AP/Cr Bank entry each time, and voiding a posted bill correctly reversing every debited account including un-claiming the VAT input recoverable amount, exactly as the code's own design intent states. (19) Credit notes → GL reversal → void (HUD-0061), preceded by a systematic sweep the HUD-0043/0060 pattern earned: grepped every line-schema-vs-processor pair in the codebase (three `build*Lines()` functions, plus every other `*LineSchema`) and confirmed no further instance exists — purchase orders already agree field-for-field, and the one other candidate (a SEAL devanning-tally schema) isn't a financial line at all. Credit notes' own schema was checked the same way and already matched. Traced live: a real invoice credited for a partial amount produced an exactly-reversed, balanced journal entry (hand-verified), a correctly-attributed activity log entry on the original invoice, and a void that mirror-reversed the credit note's own entry — a second void attempt correctly refused. Clean, reuses the invoice module's own total/tax math rather than a second copy that could drift. (20) Fixed assets → depreciation schedule → dispose/delete (HUD-0062) — clean. A 24-month straight-line depreciation schedule was hand-verified in full: the final month correctly absorbs the rounding remainder so accumulated depreciation lands exactly on the depreciable base with zero drift across two years of monthly roundings. Acquisition and delete-before-depreciation both posted/reversed correctly (cash-funded to Bank, AP-funded to Accounts Payable). A hypothesis raised by reading a self-documented code comment — that deleting a disposed asset would reverse only its acquisition entry and leave the disposal entry dangling, since both share one sourceId — was tested live rather than reported on suspicion, and disproven: the reversal mechanism correctly unwinds every entry sharing that id together, verified to net exactly zero across all four resulting journal entries. One LOW cosmetic finding, not fixed: disposing an asset before any depreciation has posted creates a real but meaningless zero-amount journal line. (21) NexusHR leave management (HUD-0063) — clean, the most thoroughly-guarded golden path traced this arc. Generated real Tanzania Employment Act statutory leave entitlements for a tenant that had never had its country set (a real gap corrected, not test data). Confirmed a real service-tenure eligibility check blocks a SICK leave submission outright (not just a display flag), a real working-days calculator correctly excludes weekends when computing days requested, an overlap guard correctly refuses a second request touching the same dates, a balance-sufficiency guard correctly refuses an oversized request while independently reporting which days in that range wouldn't have counted anyway, and approving/cancelling a request correctly moves it between pending/taken/available buckets with accurate domain events at each step. Every guard fired exactly when it should. (22) Bank reconciliation import → match → delete (HUD-0064) — clean. A real CSV bank statement (flexible Debit/Credit column parsing) correctly surfaced real, unmatched GL journal lines as match candidates within its date range, including one that exactly matched an imported line by amount; a one-journal-line-per-statement-line constraint was correctly enforced, unmatching correctly returned a line to the candidate pool, and deleting the statement correctly left the underlying, already-posted journal entry completely untouched — bank reconciliation is a working-paper layer on the GL, not part of it, and behaves that way. (23) CRM lead scoring + custom fields (HUD-0065) — clean. Hand-verified lead scoring end to end: two real rules produced exactly the expected 70/0 point split across a high- and low-value lead, a third rule pushed one lead's raw total past 100 and it correctly clamped there while the other lead's lower total stayed unclamped, and deactivating that rule immediately reverted both scores — rules are evaluated fresh per request, never cached. Custom fields round-tripped a real value correctly, and the ownership guard on `PUT /values` was verified live rather than assumed from reading the code (a nonexistent subject id was correctly refused with a 404). (24) Ondi JIT org-role grants (HUD-0066) — the security feature itself is clean: a granted role genuinely substitutes for a missing platform role, time-based expiry is real live enforcement (no cleanup job needed, verified by setting a grant's expiry into the past and watching access disappear immediately), and manual revoke cuts access off at once. Unlocking the time-boxed half of the test (the dev tenant lacked the relevant plan add-on) surfaced that the code's own SUPER_ADMIN bypass for that gate is unreachable dead code — every `/org/roles*` route excludes SUPER_ADMIN entirely — documented as a narrow, low-risk candidate, not fixed unprompted. **Closed as a same-day, user-directed follow-up (HUD-0133)**: added `SUPER_ADMIN` to all 5 routes' role gate, matching the sibling `/org/groups*` section in the same file that already includes it — the dead bypass code is now reachable exactly as written. Re-tested live: a real SUPER_ADMIN JWT now gets a clean 200 from `GET /org/roles`, still scoped to their own account's tenant via the existing `withTenant()` call, not a new cross-tenant reach. Exercising the newly-granted access then surfaced a real, unrelated HIGH bug in ComplyOS: `GET /certificates` unconditionally overwrote every certificate's status with a pure function of its expiry date, making a revoked certificate — the only lifecycle-exit this record type has — permanently indistinguishable from an active one everywhere, including its own explicit `?status=revoked` filter (which correctly found the revoked rows via the real column, then reported "active" for every one of them). Found and fixed with a one-line change; the ordinary active/expiring/expired path is unaffected. (25) Project OS (deepens the already-open HUD-0032) — attempted to trace portfolios → phases → dashboard → resources → approvals and found every one of six representative endpoints across the module's 34 routes fails identically. Root-caused it directly against the live schema: `project-os.service.ts` references columns on `project_portfolios` and `project_phases` that simply don't exist on the real tables (e.g. `owner_id`/`target_roi` vs. the real `manager_id`/`target_budget`; `sequence_order`/`gate_review_date` vs. the real `phase_number`/`progress_pct`) — comprehensive drift, not one endpoint's bug. Re-rated HUD-0032 HIGH (from LOW) to reflect the true scope now confirmed. Deliberately not fixed: a standing memory note already flags this exact module as another team's concurrent rebuild against a newer migration, with an explicit no-file-overlap boundary — this pass adds fresh live evidence of the drift's true extent, not a fix. (26) FinOps budget-vs-actuals (HUD-0067) — found and fixed a real HIGH bug: voiding any posted bill/invoice touching a budgeted GL account left a phantom variance instead of returning to baseline. Traced live: a real budget line's actuals correctly rose from 1,000,000 to 1,300,000 TZS when a new bill posted, but after voiding that same bill through the platform's own standard void-don't-delete mechanism, actuals read 700,000 instead of the expected 1,000,000 — a live-confirmed **-300,000 phantom**, root-caused to the query's `status != 'VOIDED'` filter excluding the original entry (correct) without also excluding its separate mirror reversal entry (incorrect), breaking the cancellation the reversal exists to provide. Confirmed this wasn't a repeat of a wider pattern by checking `GLService.trialBalance` (the platform's other GL-actuals consumer, which sums every line with no status filter at all and therefore nets voids correctly) and `bank-reconciliation.routes.ts` (uses the same filter, but correctly — there, a reversal is itself a real bank-matchable cash movement). Fixed by removing the filter to match `trialBalance`'s working approach; re-ran the exact original reproduction and confirmed actuals correctly return to 1,000,000 after the void. (27) FinOps GL period close/reopen/lock, MONTH and YEAR, with real corporate-income-tax accrual folded into the year-end close (HUD-0068) — clean. MONTH: a real period closed with no closing entries (just a lock), a further posting inside it was correctly refused platform-wide (the single-chokepoint guard lives in `GLService.post()` itself, not a per-route check), `FINANCE` was correctly refused the reopen (`ADMIN`-tier only) while `TENANT_ADMIN` succeeded with the reason persisted and history preserved, and posting resumed correctly afterward. Delete boundary verified both ways: a never-closed period hard-deletes cleanly; a period that was ever closed (even after being reopened) is permanently refused deletion. YEAR: posted an isolated 1,000,000/400,000 revenue/expense pair in a disjoint test year and hand-verified the entire chain before checking the API — 30% reference-default corporate tax rate (no tenant override, no fixed assets to depreciate) on 600,000 taxable income gives a 180,000 tax liability; confirmed the persisted `cit_returns` row matched exactly, confirmed the tax accrual posts *before* the closing trial-balance snapshot is pulled (so the closing sweep correctly captured the tax expense too, not a pre-tax number), and read the actual closing journal entry line-by-line: Dr Revenue 1,000,000 / Cr Expense 400,000 / Cr Income Tax Expense 180,000 / Cr Retained Earnings 420,000 (true after-tax net income) — balanced exactly. (28) FinOps withholding tax: rate → bill deduction → certificate → remittance (HUD-0069) — clean. Paid a real WHT-tagged bill in two installments specifically to test the "withholds its total WHT once across installments, never per-payment" claim: hand-computed 30,000 then 20,000 (proportional, capped by what's left), matched the API exactly, and the GL postings for both installments (a 3-line Dr AP / Cr Cash-net-of-WHT / Cr WHT-Payable entry each time) balanced to the shilling. Issued a real certificate, confirmed re-issuing it is genuinely idempotent, and read the actual PDF with `pdftotext` rather than trusting the 200 — every figure on it was correct. Batched both deductions into one remittance, confirmed a second batch attempt for the same period correctly finds nothing left, paid it, and hand-verified the GL entry clears the WHT-payable liability back to exactly zero across the whole lifecycle; a repeat payment attempt was correctly refused. (29) Store marketplace-app journey (HUD-0070) — found and fixed a real HIGH authorization gap before even reaching the golden path: `POST /apps` (submit an app for review) had no role check at all, live-confirmed that a `CUSTOMER`-portal account (a tenant's own customer, not a developer) could submit a listing straight into the platform-wide review queue — the one mutating route in the file an earlier pass's `STORE_MGMT_ROLES` fix missed. Fixed with the same CUSTOMER-block convention used everywhere else on the platform; re-verified the exact original reproduction is now refused while a legitimate staff account still succeeds. The golden path itself (submit → admin-only approval queue → approve → public catalog → tenant install/uninstall → reject correctly hides a listing from the catalog) traced clean. Also corrected a real documentation error surfaced by this trace: the platform overview had described "Store" as a "B2B procurement & equipment marketplace," but its entire backend (confirmed by grepping every frontend call site) is a software/integrations marketplace — description corrected as part of this journey. (30) FinOps deferred tax — fixed-asset timing differences, IAS 12 (HUD-0071) — clean. Created a real backdated fixed asset and hand-verified the whole computation before checking the API: a 2-year-old Class 1 (37.5% reducing-balance) asset with zero book depreciation ever posted (a known, already-documented gap — no manual "run now" route exists for the depreciation job) produces a real 9,000,000 TZS temporary difference against the 30% reference rate, landing a 2,700,000 deferred tax liability exactly. Confirmed genuine idempotency (re-running the same date posts nothing new, preserves the same journal entry id) and, more tellingly, confirmed the feature posts only the *incremental movement* between two different dates rather than re-posting the full target balance each time — a second computation a year later correctly posted just the 1,687,500 delta, with the cumulative ledger balance landing exactly on the new target. Self-caught a mistake in my own cleanup-verification script (the exact same VOIDED-status-filter trap as HUD-0067) before it became a false finding. (31) Org chart journey (HUD-0072) — found and fixed a real MEDIUM data-correctness bug: `POST /sync-staff` (import company staff into the org chart) selected every `users` row for the tenant with no role filter, live-confirmed it added two real `CUSTOMER`-portal accounts into the internal staff hierarchy with `job_title: "CUSTOMER"`. Not a security issue (no cross-tenant or unauthorized access), but a tenant's own customers being misclassified as company staff is a real, reachable correctness bug any admin hits by clicking "Sync Staff" on a tenant with active customer-portal accounts. Fixed with the same CUSTOMER-exclusion convention used platform-wide; re-verified live that the two accounts don't reappear on a fresh sync while all real staff still do. The rest of the golden path traced clean: role-gated create/update (`MANAGER`-tier) vs. a narrower delete tier (`ADMIN`+), a `JUNIOR` correctly refused mutation, and — verified live, not just read from the code — deleting a middle node correctly re-parents its children to the deleted node's own parent rather than orphaning them. Reconfirmed (not newly discovered) that org chart remains a standalone visualization feature nothing else on the platform consumes for manager resolution. (32) NexusHR biometric attendance-device journey (HUD-0073) — found and fixed a real HIGH secret-leak bug, with an otherwise genuinely sophisticated pipeline confirmed clean. `GET /attendance-devices` used `.selectAll()`, live-confirmed to leak `push_token` — the sole credential (alongside an equally-readable `serial_number`) authenticating the genuinely unauthenticated `/iclock` device-push endpoint — to every authenticated staff role, including a `JUNIOR` with zero device-management permission, directly contradicting the route's own "shown once, not retrievable again" design comment. Anyone with it could impersonate the physical device and inject fabricated attendance punches for any enrolled employee. Fixed by excluding it from both the list and update responses (confirmed the frontend never reads it from either). The rest of the pipeline traced clean using a route built specifically for this kind of testing (`simulate-punch`, which drives the exact same production code path a real device push uses): a real punch pair correctly produced a real `hr_clock_sessions` row and synced into `hr_attendance` exactly like a web/manual clock-in; an unenrolled PIN correctly landed as an orphan rather than being dropped; assigning an orphan correctly created a new enrollment and reprocessed that historical punch. (33) NexusHR overtime (HUD-0074) — clean, including a live-demonstrated race-condition guard. The statutory rate (1.5x normal, 2.0x rest day/holiday) is correctly derived from the tenant's real calendar, never accepted from the request. Built up real approved claims to exactly the 50-hour rolling four-week cap, hand-verifying the API's own "remaining" figure at every step, then live-demonstrated the exact scenario the code's own header comment names: two claims independently valid at submission time against the same remaining hours, where approving the first correctly makes approving the second fail — proving the cap is re-checked live at approval time, not just at claim time. Hand-verified the payable-amount calculation to the cent across six claims with mixed rates. No code changes needed. **Separately noted, not investigated**: `apps/api`'s typecheck now fails on `email.routes.ts`, a file this session never touched — confirmed via git history that it arrived through two commits made by a different, concurrent session's own in-progress work during this session's Phase 5 pass (that commit also happened to sweep up this session's own then-uncommitted HUD-0067 fix to `budgets.routes.ts`, confirmed still present and correct in the resulting `HEAD`). Left alone as outside this session's scope. (34) Calendar public booking-page journey (HUD-0075) — clean, the first this arc to exercise a genuinely unauthenticated public surface end to end. A real booking page's public info endpoint correctly strips tenant/user identifiers; slot generation matched a hand-computation exactly for a real 09:00–12:00/30-min page; both boundary cases (a non-working weekend day, a date past the booking window) correctly return an empty list rather than an error. The most subtle part — a 15-minute buffer extending a real calendar conflict's blocked window on both sides — was verified exactly: creating one 30-minute blocking event correctly removed three adjacent slots, not just the one it directly overlapped. Booked a slot as a genuinely anonymous caller (no auth at all), confirmed a real calendar event was created with correct guest data and host notification, then confirmed a second attempt on the exact same slot is correctly refused — proving the "re-checked at booking time, not just page-load time" double-booking guard actually holds. No code changes needed. (35) FinOps period-end FX revaluation (HUD-0076) — found and fixed a real HIGH bug. Confirmed the documented "clean baseline" design on a first-ever revaluation (zero movement, nothing posted), then found that a real, pre-existing Draft invoice — which `invoices.routes.ts`'s own code only posts to the GL when its status isn't Draft, so it had zero journal entries — was being revalued anyway, posting a real 5,223.50 TZS phantom gain straight onto the actual Accounts Receivable balance. Checked the AP side and found the identical exposure for bills sitting in DRAFT or PENDING_APPROVAL. Real, everyday impact: any tenant with unfinalized invoices/bills open during a routine month-end FX revaluation gets its real AR/AP balances silently corrupted by gains/losses that were never earned or incurred. Fixed by excluding those exact statuses, matching each document type's own real GL-posting condition. Voided the tainted mixed entry, cleared the contaminated tracking rows, and re-ran the exact same revaluation fresh — the corrected run posted only the two legitimate subjects' correct figures, with no trace of the phantom posting. Also reconfirmed the already-documented same-date idempotency guard still holds. (36) Suppliers journey (HUD-0077) — CRUD traced clean (search-by-contact-name, role-gated create vs. browse-freely list, a real soft-delete that leaves the record as `inactive` rather than removing it). Found a real MEDIUM design-level gap, same shape as HUD-0057's demurrage-liability finding: a supplier's `blocked` status is set and read only within the suppliers module itself — nothing in bill or purchase-order creation ever checks it. Live-confirmed the consequence: marked a test supplier blocked (with a "fraud investigation in progress" note) and then successfully created and posted a real 500,000 TZS bill against it with zero warning or refusal. Not fixed — whether blocking should hard-refuse, warn, or require an override is a product decision. **Closed as a same-day, user-directed follow-up (HUD-0134)**: chose hard-refuse (matching how every other "should this be possible at all" guard in this codebase behaves) and added the check to bill creation, bill editing (only when the edit changes the supplier or is the edit that posts it for the first time), purchase-order creation, and the unattended daily recurring-bill generation job (which skips the due template rather than erroring the whole batch, since there's no human there to notice). Deliberately left alone: paying down an already-existing bill, since refusing to pay a supplier you already owe money to over an unrelated dispute would be the wrong call. Re-verified live end to end: block → new bill 400s with the supplier's own note in the message → new PO 400s the same way → reactivate → the identical bill payload now succeeds. (37) Email journey (HUD-0078) — the first this arc deliberately triggered by a concurrent session's own claim rather than by picking an untraced app, verified independently rather than taken on faith: that session's new memory entry claimed a "100% completion" pass on Email, the same session whose commits briefly regressed this arc's build at journey 33. Confirmed the work is real (three new migrations, a real scheduled-send job on a genuine 15-second scheduler) before tracing it live. Came back clean across everything exercised: a real Gmail-style undo-send (confirmed a deleted still-scheduled message is structurally unreachable to the delivery sweep, not just soft-cancelled), real delivery through the platform's existing mail infrastructure with a real message id, a real read-receipt pixel that stamps a real confirmation timestamp when hit, cross-folder thread reconstruction on reply, genuine Postgres full-text search (a stemmed query matched correctly, not a substring scan), and working custom labels and bulk actions. Self-caught a false alarm along the way — an early "undo" check came back empty only because real wall-clock latency between tool calls meant the message had already been delivered by the real 15-second sweep, not a folder-filter bug. Not exhaustive (IMAP test-connection and quick-reply templates weren't exercised), so not yet re-rated in the overview artifact, but strong positive evidence for the next sync. (38) Notes journey (HUD-0079) — clean across a genuinely sophisticated per-note ACL, optimistic-locking, and legal-hold feature set. A private note is correctly invisible and unreachable to anyone but its creator; a shared note's view-vs-edit permission boundary was verified in both directions (a view-only collaborator refused an edit, then correctly permitted one after an upgrade), while the separate "only the creator manages sharing" rule held even for an editor with full content-edit rights. Optimistic locking correctly refused a stale-timestamp update with real, usable current-state data rather than a bare error, after a collaborator's edit had moved the note forward. Revision history was verified in both directions to lose nothing across two real edits and a restore. Legal hold correctly blocked permanent deletion both individually and through a bulk empty-trash sweep. No code changes needed. (39) Sign forensic case-management journey (HUD-0080) — closing a gap HUD-0045 specifically flagged as untested. Built a real signed-and-completed Sign envelope from scratch, then used the actual public, unauthenticated `/verify/compare` endpoint to submit both the genuine stamped document (a live-computed EXACT_MATCH, real matching SHA-256 hashes) and a deliberately tampered re-creation (a live-computed DOCUMENT_MISMATCH with real structural findings, not a canned response) — confirming the non-clean verdict was auto-opened as a forensic case by the system itself, unprompted, exactly as designed. Found and fixed a real LOW bug: manually opening a case with a note produced two identically-timestamped 'opened' chain-of-custody rows instead of one, because the route fired a second redundant event just to attach the note rather than folding it into the case-opening service's own internal logging. For a system whose entire purpose is an accurate, legally-defensible custody trail, that's a genuine (if minor) correctness defect; fixed by threading the note through the same call, re-verified live to produce exactly one row. Confirmed re-analysis versioning never overwrites a prior run, report generation correctly appends to the evidence manifest, and every view/export is genuinely logged as its own custody event. (40) FinOps dividends (HUD-0081) — clean. Declared and paid a real dividend, hand-verifying both GL entries (Dr Retained Earnings/Cr Dividends Payable at declaration, Dr Dividends Payable/Cr Cash at payment) and confirming a repeat payment is refused. Confirmed the real reason this feature exists — the statement-of-changes-in-equity report's dividend-movement attribution — correctly isolated the -5,000,000 TZS dividend movement from unrelated same-day noise (a prior journey's void-cleanup reversal, itself already net-zero) rather than conflating the two. No code changes needed. (41) NexusHR disciplinary case management (HUD-0082) — clean. The full status lifecycle (open→in_progress→resolved→reopened) correctly stamps and clears `resolved_at` at each transition, including the reopen case specifically clearing a stale timestamp rather than leaving it behind. Confirmed the file's deliberate MGMT-roles-only scope (case data is manager/HR working material, not something the subject browses themselves) actually holds live. No code changes needed. (42) Sign matters + jurisdiction engine (HUD-0083), closing out the Sign cluster's two remaining small surfaces — clean. Matters is deliberately not its own table, just a live `GROUP BY` over envelopes' matter references; created two real envelopes sharing one reference and confirmed the aggregation (count, distinct client names) and drill-down both worked correctly, with the same cross-user-disclosure RBAC gate as the rest of Sign. The jurisdiction engine returned real, specifically-cited Tanzania Electronic Transactions Act sections for all four execution types, correctly resolving the tenant's own jurisdiction server-side — genuine legal reference data across all four EAC countries, not placeholder text. No code changes needed. (43) Bliss escalations (HUD-0084) — found and fixed a real LOW error-handling gap: a `CHAT`-type escalation against a `channelId` that doesn't exist (a plausible real scenario — a channel deleted between the frontend loading it and the user submitting) fell through to a raw Postgres foreign-key violation, surfacing as an unhelpful generic 500 instead of a clean 404. Fixed with the same "check the reference exists first" guard this codebase already applies elsewhere (e.g. HUD-0082's employee check); re-verified the exact reproduction now 404s cleanly while a real channel still works. The rest of the golden path traced clean with two genuinely different JUNIOR accounts: own-only visibility correctly isolates each from the other while a SENIOR resolver correctly sees both; notification fan-out correctly reaches every resolver except the escalator themselves, checked across both escalators; the one-step status machine and its terminal-state refusal both hold. (44) AgencyHost referral commissions (HUD-0085) — clean, including live fraud-detection verification. Created two real referred tenants and invoked the actual commission-computation service directly (real code, real writes) to simulate a first-payment event without a payment-gateway round trip: a clean referral correctly produced a 10% pending commission; a referral whose signup phone was a reformatted duplicate of a real staff member's own phone was correctly flagged, confirming the phone-normalization self-referral check actually catches a reformatted number, not just an exact string match. Confirmed a second computation call for the same referred tenant is a genuine no-op (idempotent by unique index), not a duplicate or silent overwrite. Traced the full superadmin lifecycle live: approve, an honest 409 refusal for automatic payout (no gateway connected, correctly not faked), a real manual payout, reject, and the already-decided guard refusing a second decision on either commission. No code changes needed. (45) HuduBI platform data-quality engine (HUD-0086) — the first journey this arc to verify a diagnostic tool's own detection logic against deliberately-planted real defects rather than just its happy path — clean. A baseline run before planting anything already surfaced real findings from this arc's own testing activity. Deliberately planted three real, realistically-shaped anomalies via SQL (a future-timestamped domain event, a stage-history row exiting before it entered, an expired-but-still-verified certifier) and confirmed the checker's SQL logic caught every one with the exact right sample id, count, and tenant. Discovered along the way that one check's own description overstates its reachability: a genuine foreign key means an "orphan" metric alert rule can only arise from a metric deprecated after the fact, never from one that never existed — confirmed live via a failed insert rather than assumed from reading the code. Verified the self-healing design live: once the planted rows were cleaned up, the very next run correctly stopped reporting them with zero special handling. No code changes needed. (46) Cross-app related-records lookup (HUD-0087) — clean, including a critical live cross-tenant isolation check. The generalized "what's linked to this record" panel correctly returned only relations with real rows for both a shipment and a customer, independently confirmed against Postgres that the omitted relations were genuinely empty rather than lost data. Most importantly: looked up a real shipment id belonging to a *different* tenant and confirmed the API correctly 404s rather than leaking that tenant's linked-record data — the tenant scoping actually holds under a real cross-tenant id, not just in the abstract. A CUSTOMER JWT is correctly refused the entire endpoint, exactly matching the file's own documented reasoning for why. Purely read-only journey, no cleanup needed. (47) NexusHR benefits administration (HUD-0088) — clean across the full plan and enrollment lifecycle. Waiving an enrollment correctly left `terminated_at` null (verified against the row, not just the response) — confirmed the column is only stamped for an actual `terminated` status, not any non-enrolled state generically. Re-enrolling into the same plan after waiving went through a real `onConflict` upsert, confirmed via a direct row count that exactly one enrollment row exists afterward, not a duplicate. Both authorization boundaries (a non-MGMT employee acting on someone else's enrollment, at creation and at status-change) were correctly refused with 403, and plan creation is correctly refused to a JUNIOR. Soft-deleting a plan correctly removed it from the active list while both real enrollment rows stayed fully queryable and correctly attributed, and a fresh enrollment attempt against the retired plan was correctly refused. No code changes needed. (48) Road consignments + warehouse dock-appointments (HUD-0089) — the golden path itself is clean (trip/border/consignment status transitions all correctly stamp their own timestamp fields; role gates hold: JUNIOR refused consignment creation, CUSTOMER refused the whole surface, operational transitions deliberately open to any staff role). Found and fixed a real MEDIUM bug spanning the whole file: every id-lookup/mutation in `consignment.service.ts` used `.executeTakeFirstOrThrow()`, so a wrong/stale id crashed with a raw 500 instead of a clean 404, across all six affected routes — two of them (adding a trip or border crossing against a bad consignment id) crashed even harder, falling straight into a real FK constraint. Verified this was never a security gap first: read `pg_policies` directly and confirmed genuine, correctly-scoped RLS (with FORCE ROW LEVEL SECURITY) backstops every one of these routes' missing explicit tenant filter, live-confirmed with a real second tenant's JWT against this journey's own ids — correctly refused, never leaked. Fixed by switching to `.executeTakeFirst()` plus an existence check before insert; re-verified all six original crashing reproductions now 404 cleanly. (49) NexusHR training & certification (HUD-0090) — clean, the first live trace of a feature the audit had previously confirmed entirely absent. Completing a certification enrollment stamped a `certificate_expiry_date` matching an independent hand-computation of the exact same date formula to the day, correctly snapshotted onto the enrollment rather than left to drift with the course's own settings; failing a non-certification course's enrollment correctly left the expiry field null. Both authorization boundaries (enrolling or cancelling someone else's enrollment without MGMT) were correctly refused, and the expiring-certifications report correctly surfaced only the one real completed certification while omitting a cancelled and a failed enrollment. Retiring both test courses correctly preserved all enrollment history. No code changes needed. (50) NexusHR onboarding/offboarding checklists (HUD-0091) — clean, and the strongest-evidence test in this cluster: rather than testing the per-person-checklist generator in isolation, drove it through the actual production path twice — accepted a real invitation through the genuinely public `POST /auth/accept-invite` (firing a real `user.joined` event) and confirmed a real onboarding checklist auto-generated with the tenant's configured items, then deactivated that same employee through the real staff-status endpoint (firing `hr.staff_deactivated`) and confirmed a second, independent offboarding checklist auto-generated from a different template — proving the subscriber correctly branches by event/checklist type rather than reacting to "any HR lifecycle event" generically. Checking items off correctly attributed the acting manager, auto-completed the checklist on the last item, and auto-reopened it (clearing the completion timestamp) when an item was unchecked afterward — a genuinely bidirectional status derivation. No code changes needed. (51) SEAL warehouse equipment & maintenance (HUD-0092) — the derived-alert design (due_soon/overdue/out_of_service override) and the maintenance-record-as-source-of-truth cascade both work exactly as designed. Found and fixed a real MEDIUM bug, same family as HUD-0089: `PATCH /equipment/:id` crashed with a raw 500 instead of a 404 for a bad id, and `POST /equipment/:id/maintenance` was worse, falling into a real FK constraint and leaking its internal name in the response. Both queries already had an explicit tenant clause, so this was a status-code/verbose-error bug, not a tenant-isolation gap. Fixed with the same `.executeTakeFirst()` + existence-check pattern already established; re-verified live. (52) SEAL zone-occupancy sensors (HUD-0093) — clean, and the first journey this arc to verify a live WebSocket broadcast with a real connected client rather than trusting the call site. Connected a real `ws` client to `/ws`, ingested three real sensor readings deliberately out of chronological order, and confirmed all three genuinely broadcast over the socket with correct tagging, while the "current reading" query correctly picked the reading with the latest `recorded_at` timestamp rather than the one inserted last — proving the ordering is real, not an insertion-order artifact. A useful same-cluster contrast with HUD-0092: this file's ingestion path already had the "check the device exists first" guard done right, confirming the sibling bug wasn't a pattern repeated blindly across the whole SEAL module. No code changes needed. (53) SEAL automation rules (HUD-0094) — the trigger/dedup mechanics are real: created a real low-stock rule and lot fixture, confirmed evaluation fires against every genuinely qualifying lot in the tenant (not just the fixture), confirmed a real task was created with correct fields, and confirmed a second evaluation immediately after correctly fired zero times, proving the per-rule-per-subject dedup guard actually prevents duplicate work on a repeat click. Found and fixed a third instance of the HUD-0089/HUD-0092 pattern (`PATCH /automation-rules/:id` crashing 500 instead of 404 for a bad id); re-verified live. Ran the platform-wide grep this pattern's recurrence warranted rather than just recommending it — surfaced roughly 30 files, most defended by a prior existence check on spot-check and therefore not live bugs, correctly scoped as its own future sweep rather than something to absorb into this journey. (54) AgencyHost managed-client onboarding + cross-agency isolation (HUD-0095) — clean, and the strongest cross-tenant test this arc has run on a two-sided relationship rather than a simple owned-record check. Created a real, genuinely independent client tenant through the actual onboarding-adjacent flow and confirmed every resulting row across three tables agreed. Most importantly: a completely different tenant was correctly refused 404 (not a leaking 403) reaching the client's resources, and — after the managing agency detached the client — that same agency also immediately lost access on its very next request, proving the access check re-verifies the relationship's live status on every call rather than caching an attach-time grant. A deploy attempt against a freshly-provisioned tenant was honestly refused for lacking a CI provider, and a health check against a fictitious domain genuinely failed DNS resolution rather than faking a result. No code changes needed. (55) HuduBI widgets + cross-app entity resolution (HUD-0096) — found and fixed a real LOW bug: a nonexistent widget id crashed `updateWidget()` and the route mapped every error to 400, misreporting a not-found as a malformed request; fixed to reuse the same "Widget not found" → 404 convention the sibling `GET .../data` route already used, re-verified live. A code-reading security hypothesis about the cross-app entity resolver's fuzzy email-match join was tested live with a real cross-tenant email collision rather than reported on suspicion — and disproven: the join is safe in practice because a recipient row and its parent envelope are always in the same tenant by construction. No leak occurred. **(HUD-0097)** Executed the platform-wide sweep flagged as a follow-up at HUD-0094: read every one of ~90 `.executeTakeFirstOrThrow()`-on-a-URL-id call sites across ~40 route files individually. The overwhelming majority (hr.routes.ts's 15 hits, task-projects.routes.ts's 7, and roughly 20 other files) were already correctly guarded by a prior existence check or an access-resolution helper — confirming the earlier bugs were one-off mistakes, not a habit. Found and fixed 17 real unguarded instances across 5 files (cargoLoading.routes.ts, inventory-counts.routes.ts, seal-fulfillment.routes.ts, support.routes.ts, vehicleDetail.routes.ts), each live-reproduced as a genuine crash before the fix and re-verified as a clean 404 after. **Addendum**: the original grep required the id-check and the throwing call on the same line, silently missing any two-line chain — a shape this codebase uses constantly. A multi-line-aware re-run surfaced 16 more files and 30 more real unguarded instances (fleetOps.routes.ts alone had 5; warehouse.routes.ts's own dock-appointment transitions, already traced happy-path-only in HUD-0089, had 4 more), one of them worse than the rest: billing.routes.ts's default-payment-method route cleared every other method's default flag *before* checking the target id existed, so a bad id used to leave a tenant with no default payment method at all, not just a bad response. HUD-0097's combined total: 47 real bugs fixed across 21 files, the largest fix count of any entry in this arc — and a concrete lesson that a grep sweep's completeness depends entirely on its pattern matching how the codebase actually formats the call chain. (56) SEAL reefer monitoring + yard slotting (HUD-0098) — clean, tracing the same file HUD-0097's addendum found two bugs in, now confirmed correct on its own real golden path: a ±2°C tolerance band correctly classified three real readings including one exactly on the boundary (inclusive, not off-by-one), yard-slot occupancy is a live join-derived count that correctly went 0→1→0 across assignment and unassignment with no separate bookkeeping, and a made-up container number was correctly rejected by a real ISO 6346 check-digit validation before a valid one succeeded. No new bug found. **(HUD-0099)** Re-verifying `seal.routes.ts` for HUD-0098 surfaced two more blind spots in HUD-0097's own sweep methodology, not in the codebase: a file appearing in *both* the single-line and multi-line grep passes had been treated as fully checked once any one known line was re-confirmed, when the two passes' match counts for that file actually differed — and the sweep had only ever grepped `apps/api/src/routes`, never `apps/api/src/services`. A per-file match-count audit across all 33 files from HUD-0097's addendum list, followed by a full manual read of the highest-discrepancy files, found 19 more real "crash instead of 404" bugs: `seal-billing.service.ts` (the lot lookup behind both `previewAccrual`/`generateStorageInvoice`, called from `seal-billing.routes.ts`), `seal.routes.ts` (3 more routes), `hr.routes.ts` (8, including `PATCH /staff/:id/status` — the exact route HUD-0091's onboarding/offboarding journey exercised cleanly every time, because that trace always supplied a real id), `support.routes.ts` (2 more), and `ondi.routes.ts` (5, mirroring the same sensitive user-role/status shape as the HR bugs). `tasks.routes.ts` was re-checked in full afterward and is clean — every remaining call site is either an insert or already guarded by `resolveTaskAccess()`. All 19 fixed with the same shape as HUD-0097 and live-verified both ways (synthetic nonexistent id → clean 404; a real id → unchanged happy path). HUD-0097+0099 combined: 66 real bugs across 27 files. **(HUD-0100)** Spot-verifying HUD-0099's `seal.routes.ts` fix a second way (a well-formed body with no field the route recognizes) surfaced an unrelated bug in the same handler: an all-unrecognized `PATCH /locations/:id` body left the update's `SET` clause empty, and Kysely's `.set({})` emits invalid SQL (`UPDATE … SET WHERE …`), crashing with a raw 500 instead of a 400. Fixed with an explicit empty-patch guard; not swept for elsewhere in the codebase. (57) SEAL bonded-storage billing golden path (HUD-0101) — completed the trace this arc had left unfinished: configured real rates on two new compartments (flat-per-lot and per-CBM, the first time either method had ever been set on this tenant), created three backdated lots, and hand-verified `GET .../storage-accrual` and `POST .../generate-storage-invoice` exactly for both billing methods (100,000 TZS and 175,000 TZS totals, both matched to the shilling), confirmed the `LotHasNoVolume` guard on a lot with no volume, and proved the `storage_billed_through` watermark live — re-invoicing immediately after returned `days: 0` and was correctly refused as `NothingToBill`. **Found and fixed a real HIGH bug** while testing the authorization boundary: `seal-billing.routes.ts` has only an entitlement check, no role gate — unlike its sibling SEAL route files, both of which block `CUSTOMER` from the whole surface per the original HUD-0024/0031 sweep. This file postdates that sweep (built for HUD-0099's own billing feature) and never got it. Live-confirmed a `CUSTOMER` JWT for a *different* customer than a lot's owner could read that lot's confidential billing figures and — worse — actually create a real Draft invoice against it, consuming the lot's billing watermark with zero ownership check. Fixed with the same `CUSTOMER`-block preHandler used platform-wide; re-verified the exact reproduction now gets `403` while staff access is unchanged. (58) Contacts nested-label hierarchy + smart groups, verified live in a real browser for the first time (HUD-0102) — closes the "frontend not re-checked visually" gap flagged since the feature's original 2026-09-09/10 build. Logged into the real dev server as the seeded TENANT_ADMIN, created real contacts/labels/a smart group through the actual UI, and confirmed all five originally-open questions: the sidebar renders a real nested label tree with working expand/collapse; "Add sub-label"/"Move to…"/rename all function correctly from the label's dropdown; the Smart Groups section's "+" routes to a properly-rendered rule builder (field/operator/value selects, working "Add rule"); and opening a real smart group shows its live, server-evaluated matched-contact list (2 of 3 test contacts matched correctly by company, the third correctly excluded) with a live count badge in the sidebar. **Clean — no bug found.** All test data (a stray duplicate contact from a flaky test script, along the way — not a product bug) cleaned up via the app's own real delete flows. (59) Ondi's real SAML 2.0 SSO — `ondi-saml.routes.ts`'s real XML-DSig assertion handling, never traced despite making several specific cryptographic/security claims in its own header comment (HUD-0103). Built a genuine adversarial IdP simulator rather than trusting the claims: a real self-signed RSA/X.509 keypair, a real `samlify.IdentityProvider` signing real assertions, registered against a real `sso_providers` row through the actual admin API. Nine scenarios run, every one behaved exactly as documented: a valid IdP-initiated login succeeded with a genuine, independently-decoded session (right user, right tenant, right role); a tampered assertion, one signed with the wrong key, one with a mismatched audience (the specific gap `samlify` itself leaves open per SAML Core §2.5.1.4, closed by this file's own manual check — confirmed the check actually fires, not just present in the diff), and one with an unknown email were all correctly rejected with the exact matching failure reason recorded in the audit trail each time; cross-tenant isolation held (a real user existing only in a different tenant, referenced by a validly-signed assertion for this tenant's own SP, was correctly refused); SP-initiated replay protection held (the identical assertion accepted once was correctly rejected on a second submission); and revoking the tenant's SSO entitlement mid-session correctly blocked an otherwise-perfectly-valid assertion. **Clean — no bug found**, the most adversarially-tested "clean" result in this arc: nine attack scenarios against a real cryptographic boundary, not a happy-path trace. (60) Ondi's real OAuth 2.0/OIDC provider (`ondi-oauth.routes.ts`, HUD-0104) — the direct continuation of HUD-0103's methodology, since this file makes exactly the same class of security claims (RS256 signing, PKCE, single-use codes, refresh rotation, revocation) and had never been traced. 16 adversarial scenarios run against the platform's real seeded public client and a real registered confidential test client: PKCE mandatory-for-public-clients, wrong-verifier rejection, authorization-code single-use, redirect_uri binding, wrong-client-secret rejection, refresh-token single-use rotation, cross-client refresh-token binding, token tampering, an `alg:none`-style forged-token attempt, and immediate revocation propagation to both `/userinfo` and `/introspect` all behaved exactly as intended — 15 of 16 clean. **Found and fixed a real LOW bug**: `/introspect` authenticated its caller as a registered OAuth client but never checked whether that specific client was the token's own intended audience, so any registered client could introspect a token minted for a *different* client and see its owning user, the other client's identity, granted scopes and timestamps — live-confirmed, then fixed with the same anti-enumeration shape (`active: false`) already used for a revoked or expired token, re-verified correct in both directions. (61) CRM smart views + the SEAL↔CRM bonded-warehouse link (HUD-0105) — `crm-smart-views.routes.ts`'s real per-entity SQL-predicate compiler (10 operator kinds across text/num/uuid/bool/date/label, mirroring Contacts' own smart-groups verified in HUD-0102) and `seal-crm-link.routes.ts`'s customer↔lot bridge, neither previously traced. Created real leads with deliberately distinct values, a real label, and a converted deal, then built views exercising every operator family — every count and every returned row matched a hand-computed expectation exactly, including one apparent mismatch that traced cleanly to a genuine pre-existing test lead rather than a defect. Strict validation correctly rejected an unknown field and a wrong-for-that-field operator with specific messages; `PATCH` correctly re-evaluated a view's live count after its rules changed; `DELETE` genuinely removed a view. The SEAL↔CRM link correctly returned real bonded-warehouse lots with the right fields, required `owner_id`, and refused a `CUSTOMER`-role JWT. **Clean — no bug found.** (62) CRM activity timeline + real send-email-from-CRM (HUD-0106) — `crm-activity.routes.ts`, never traced. Created two leads (one with an email, one without), converted one to a deal, and exercised the full matrix with four real user identities. Recipient-resolution precedence was live-flipped mid-test: a deal's send-email correctly used its lead's email until a real customer was attached via `PATCH`, then correctly switched to the customer's email on the identical route — directly proving the code's "customer first, once one exists" comment. A lead with no email was cleanly refused. Delete authorization held at the real boundary: an unrelated non-manager's delete attempt on another user's note returned `204` but genuinely deleted nothing (confirmed the note survived), while a manager's delete of the same note actually removed it; a system-logged event was correctly undeletable through this route for *any* role, including `TENANT_ADMIN`. Cross-subject isolation and nonexistent-subject 404 handling both confirmed correct. **Clean — no bug found.** (The shared dev API server was found already stopped, unrelated to this session's own actions; the trace ran on a temporary isolated instance instead, left the shared server exactly as found rather than restarting a process this session doesn't own.) (63) Platform Packages admin CRUD (HUD-0107) — `packages.routes.ts`, flagged since 2026-08-24 as "looked clean on read, not separately traced." Confirmed the public/all-packages split, role gating on every write route, and the full create→patch→soft-delete lifecycle all behave correctly. **Found and fixed a real LOW bug**: creating a package with an already-used `code` had no local error handling at all and crashed as a raw, sanitized `500` instead of a specific `400` — the exact "duplicate key reported as an opaque 500" shape two sibling CRM routes in this same codebase already guard against. Fixed with the identical established pattern; re-verified the original reproduction now returns a clean `400`. (64) Real Bitcoin/OpenTimestamps ledger anchoring for SEAL and ClearOS declarations (HUD-0108) — `seal-ledger-anchor.routes.ts` and its ClearOS sibling submit a real checkpoint hash of the tenant's tamper-evident ledger to the public OpenTimestamps calendar servers, anchoring it to Bitcoin — an external, Hudumika-independent proof verifiable by anyone, never previously traced. **Found and fixed the same HUD-0097-class bug copy-pasted into both files' service layer** (`checkAnchorConfirmation`'s unguarded `executeTakeFirstOrThrow()` on a URL id, crashing as a `500` instead of a `404`); fixed identically in both, re-verified live. Then verified every cryptographic claim independently rather than trusting the API: hand-computed the exact checkpoint-hash algorithm against the raw ledger tables before calling the API, and the real anchor call — a genuine 3.4-second network round-trip — returned the identical hash; the downloaded `.ots` proof file's raw bytes were inspected directly, confirming the real OpenTimestamps magic header followed immediately by that exact same hash. Repeated independently for ClearOS declarations with the same result. `NothingToAnchor`, the pending/not-yet-Bitcoin-confirmed state, and the `CUSTOMER`-role block all behaved correctly on both files. **The most cryptographically-verified journey in this arc** — nothing about the external proof was taken on trust. (65) SuperAdmin Query Builder — a real raw-SQL execution tool (HUD-0109), by far the highest-blast-radius surface examined in this arc and never traced. Attacked every claimed layer of its defense-in-depth design directly rather than trusting the code comments: connected to Postgres as its stated backstop role (`hudumika_readonly`) by hand and confirmed INSERT/UPDATE/DROP all genuinely fail with real Postgres permission errors, independent of any application logic; confirmed the visual-query allowlist rejects both a disallowed table and a disallowed column, and that injection payloads placed in filter values are safely parameterized while payloads smuggled through identifier fields (table/column/order-by) are rejected outright by exact-match allowlist checks; drove the real time-boxed OTP grant end to end — retrieved the actual sent email from the outbox, extracted the genuine code, confirmed a wrong code fails without consuming it and the real code is single-use (an immediate replay is refused); with a live grant, confirmed multi-statement, non-SELECT, and keyword-blocklisted (case-insensitively) raw queries are all rejected; proved the 10-second statement timeout is real by running `pg_sleep(15)` and watching Postgres itself kill it at ~10.4 seconds; and proved the auto-expiring grant is real by backdating its stored expiry directly in the database and confirming both `/settings` and `/raw-run` correctly treat it as expired despite a stale `true` flag still present. **Clean — every one of ten defensive claims held**, including the one (the Postgres role's real permissions) no prior journey in this arc had verified by actually connecting as that role. (66) Inbound public webhooks — GPSWOX fleet tracking + Meta WhatsApp Cloud API (HUD-0110) — the platform's real, deliberately unauthenticated-by-design receiver for two third-party push integrations, carrying its own HMAC signature verification and redelivery-dedup logic, never traced. Configured real test secrets on an isolated instance (both fail open when unconfigured, so verifying the *enforcing* path required this) and attacked the Meta HMAC check four ways: correct signature accepted, wrong-secret signature rejected, missing signature rejected, and — the key case — a payload with its original valid signature but tampered content afterward was correctly rejected, confirming the check verifies exact raw bytes captured before parsing. GPSWOX's shared-secret token was similarly correctly enforced. Verified the full GPSWOX functional flow against real rows: a position update correctly created a `vehicle_positions` row with every field parsed right, and a geofence alert correctly fanned out real notifications to every fleet-manager-role user. Proved Meta's documented at-least-once redelivery is genuinely deduplicated: sent two real messages then replayed both, and the database showed exactly 2 stored messages, not 4. Verified keyword auto-reply (case-insensitive exact match, a real logged reply) and non-text message placeholder generation, confirming the latter correctly does not trigger the keyword engine. **Clean — every claim held.** (67) Ondi's real login front door — phone-OTP, magic-link, passwordless-TOTP, WebAuthn/passkey, and Google/Microsoft/Apple federation (HUD-0111) — five sign-in mechanisms landing on the same real session issuance, never traced despite its sibling SAML/OAuth *provider* surfaces already covered (HUD-0103/0104). Stood up an isolated instance against a hand-written mock Redis to exercise the Redis-backed mechanisms (OTP/magic-link/passkey) as *enforcing*, not just fail-closed. **Found and fixed a real LOW enumeration bug**: `/otp/request` returned a distinct `502` instead of the generic enumeration-safe response whenever a registered phone's tenant had no SMS gateway configured (the dev tenant's actual current state, not a contrived edge case) — the exact leak this same file's own comment says it already closed for the no-match case, and one its sibling `/magic-link/request` in the same file already avoided correctly. Fixed by mirroring that sibling's swallow-the-delivery-failure pattern; re-verified live that a registered and an unregistered phone now get byte-identical responses. Everything else held under direct adversarial testing: OTP's 5-attempt lockout, single-use codes, and live risk-scoring all fired correctly; magic-link's token survived both a missing *and* a wrong TOTP code without being consumed (only the easy case is usually tested); passkey's enumeration-safety was confirmed byte-identical between an unregistered email and a registered one with zero passkeys; Google's `/verify` made a real call to Google's own tokeninfo endpoint and correctly rejected a garbage credential, proving genuine external verification rather than a stub; Microsoft/Apple correctly report unconfigured. Sixty-seven journeys of many — several other apps' core journeys remain untraced. (68) Ondi's mutual-consent trusted-contact account recovery (HUD-0112) — the "I lost my password AND my email" path, split across an unauthenticated requester half and an authenticated contact half, never traced. Set up a real recovery-contact relationship between two real accounts and drove the whole golden path live. **Found and fixed a real HIGH bug**: the feature was completely unreachable for its entire purpose — the one token `/auth/recovery/complete` requires was never surfaced anywhere to any human, not in the requester's own (correctly enumeration-safe) response, not in the notification email sent to the contact, not in the contact's own request list or approve response, despite the frontend's own header comment describing the intended design as "the link a contact would share back." Proved the rest of the machine was completely sound first — retrieved the real token directly from the database, backdated the 24-hour cooldown the same way HUD-0109 tested the Query Builder's OTP grant, and completed a real password change, logging in with the new password successfully — before fixing the actual gap: the token is now included in the contact's request list and approve response, and a "Copy Link" action was added to the contact's own review UI so they have something concrete to relay back out of band. Rated HIGH rather than CRITICAL since it fails safe — the account simply stays inaccessible via this one path, nothing is exposed or bypassed — and every other recovery path (email reset, OTP, magic-link, TOTP, SSO) is unaffected. Sixty-eight journeys of many — several other apps' core journeys remain untraced. (69) Ondi's auto-join-by-domain workflow (HUD-0113) — an outsider who signs up with an email matching an existing tenant's staff domain is offered "join this workspace" instead of a new tenant, queues for admin review, and only gets a real account once approved with an admin-chosen role. Never traced. Confirmed domain-matching, race-safe duplicate-request rejection, and server-side re-verification of the domain match against a tampered client-supplied tenant id; confirmed both tenant admins were genuinely emailed; confirmed self-escalation is closed at the schema level (SUPER_ADMIN isn't an acceptable approval role at all); approved a real request and confirmed the new person could log in with the exact password they originally submitted; denied a second request and confirmed no account was created and the reason reached the real notification email; and — the one angle this file's tenant-scoped queries hadn't been proven against — confirmed a real admin from a completely different tenant saw an empty queue and got an identical generic 404 attempting to approve or deny the other tenant's real request by id, with no distinguishable signal leaking its existence. **Clean — no bug found.** Sixty-nine journeys of many — several other apps' core journeys remain untraced. (70) Real-time session/device revocation, idle-timeout enforcement, and a deactivation-triggered "leaver" access-revocation automation (HUD-0114) — never traced. The core question: does "Sign Out" actually kill a still-valid JWT immediately, or is it a cosmetic database flag? Logged in for real, self-revoked that exact session, and immediately retried the same still-unexpired access token — rejected — then confirmed the matching refresh token was rejected too, so a stolen refresh token can't quietly mint a new access token after a sign-out. Confirmed "sign out everywhere else" correctly preserves the caller's own session while killing every other one. Found and traced a second mechanism the routes themselves gave no hint of: deactivating a staff account fires a domain-event subscriber that automatically revokes every one of that person's active sessions, org role grants, and OAuth app consents in the same request — confirmed live by deactivating a real account mid-session and watching its still-valid token die instantly, then confirmed the resulting audit trail (`ondi_automation_log`, a `session_revoked` event) was genuine. Live-tested the idle-timeout policy the same way — set a tenant policy, backdated a real session's last-activity timestamp, and confirmed a cryptographically-valid, not-otherwise-expired token was still correctly rejected as idle. Confirmed real per-user scoping (not just per-tenant): a genuine admin JWT couldn't rename a different user's own device by id, getting the same 404 a nonexistent one would. **Clean — no bug found.** Seventy journeys of many — several other apps' core journeys remain untraced. (71) CRM labels + the cross-app customer/lead search picker (HUD-0115) — two small, previously-untraced route files: a polymorphic label taxonomy shared across leads/deals/customers, and the real search endpoint backing both CRM's own picker and ClearOS's landed-cost calculators, with every real query logged. Confirmed RBAC differs deliberately between the two files (FINANCE can search but not manage labels) rather than by oversight; confirmed a duplicate label name gets a clean 400; confirmed the label-assignment subject lookup is genuinely tenant-scoped by attempting to assign a label to a real lead in a different tenant (refused); confirmed cross-tenant IDOR on the label itself is blocked both ways (a seeded label in a different tenant survived a patch/delete attempt untouched); confirmed deleting a label that's still assigned genuinely cascades the mapping row away, not just in theory; and confirmed the search log only ever records a real typed query, never the picker's own "list everything on focus" call. **Clean — no bug found.** Seventy-one journeys of many — several other apps' core journeys remain untraced. (72) Contacts' Google/Microsoft OAuth sync (HUD-0116) — never traced, and a real OAuth consent screen isn't reachable from this environment, but everything up to and past that step was still fully testable, including forcing the real failure path. With no credentials configured, both status endpoints honestly reported unconfigured and every action endpoint returned a clean 400. Configured real-shaped tenant-wide OAuth credentials through the real settings endpoint and confirmed the stored secret round-trips through encryption correctly — the generated authorization URLs carried the genuinely decrypted client id and correct scopes, confirming a previously-documented decrypt bug stays fixed. Seeded two real per-user connections (one with an expired token needing refresh, one with a fresh-but-fabricated token) and triggered sync on both: each one made a real network call to Google's actual production servers and came back with Google's own real rejection message — proof these paths call the real API rather than stub out under test. Confirmed per-user isolation: each connection was visible only to its own owner, and disconnecting one user's account left a colleague's completely untouched. **Clean — no bug found.** Seventy-two journeys of many — several other apps' core journeys remain untraced. (73) The Developer Platform (HUD-0117) — 15% on the readiness dashboard, the platform's lowest-rated app, never traced; the low rating turned out to undersell how large the surface is and overstate how contained the risk was. **Found and fixed a real CRITICAL cross-account/cross-tenant authorization bypass**: none of 13 authenticated account-/project-scoped console routes ever verified the caller owned the account or project named in the URL. Live-proved it with two real accounts belonging to two different users in two different tenants — the second user's real login could list the first user's real projects, read their real prepaid billing balance, and mint a brand-new, unrestricted-scope, live production API credential against the first user's own project, which then genuinely authenticated against the live API gateway. Fixed by adding an ownership/membership check to every one of those routes, including a narrower variant where a caller could pass their own real account alongside a different developer's real project in the same request; re-ran the exact original reproduction and confirmed it's now refused while the legitimate owner's own access is unaffected. **Found and fixed a real HIGH bug**: inviting a real, existing colleague to a developer account was completely non-functional — the lookup queried a database connection with no tenant context set, so a security policy silently returned nothing for any email, always; fixed by using the correct connection for this identifier lookup, the same pattern the login and signup flows already use for this exact situation. Also closed a smaller input-validation gap that let an invalid role reach a database constraint and surface as an unhelpful generic error instead of a clear one. **Found and documented (not fixed) a real HIGH issue**: the entire API-gateway product catalog returns fabricated, input-independent responses presented as genuine — a "verify this digital signature" endpoint that returns valid for a nonexistent signature and garbage input every time, and a "verify this business registration" endpoint that returns the same fabricated company officers regardless of what's actually submitted. This is a product-scope decision to build the real integrations behind these products, not a bug fix, and is documented with exact reproductions rather than silently rebuilt. **Also found and documented a real MEDIUM issue**: three separate successful gateway calls produced zero usage-metering records, meaning the billing pipeline these products are supposed to run on does not appear to be working. **Both closed as a same-day, user-directed follow-up (HUD-0136)**, this time with something the original trace lacked — an isolated instance with its own captured console output, which finally surfaced the metering bug's real cause: a query that resolves the wrong table's id (the join row's own primary key instead of the actual provider's), so every single gateway call's usage-event insert threw a foreign-key violation that was silently swallowed. Fixed the id reference; live-verified a real usage event and a real provider-settlement row now both get created correctly. For the fabricated data: the landed-cost product now calls the platform's own real, HS-code-driven customs duty engine (the same one ClearOS and SEAL already use, hand-verified to the shilling earlier this arc) instead of a hardcoded 25%/18% guess; the digital-seal product now genuinely persists what it issues and checks a real digest match instead of always saying yes, honestly dropping a fabricated signature-algorithm claim that was never backed by any real cryptography; and the business-verification product was discovered to have a genuine, already-built live-scraper twin sitting in ComplyOS's own codebase (never found by the original trace) — extracted into a shared function and wired in, so it now returns real BRELA registry data when the portal is reachable and an honest "could not reach the registry" answer when it isn't, never a fabricated company officer list. Seventy-three journeys of many — several other apps' core journeys remain untraced. (74) Onsite AgencyHost's cross-tenant agency-manages-real-client-tenants surface (HUD-0118) — the exact same shape of risk as the previous journey's Developer Platform finding (a URL id naming a different tenant's resource, gated only by the caller's own tenant identity), chosen deliberately to check whether that failure was systemic or local. Created a real, independent client tenant and drove the full golden path against it as the managing agency — a domain, a DNS record, an application, an honestly-refused deployment attempt with no CI provider connected. Then attacked the access-control middleware three ways: a genuinely unrelated third tenant was refused reading or writing the same client's resources; the client tenant itself was refused reaching this same management surface for itself, since the design only ever grants the agency side of the relationship; and detaching the relationship immediately cut off further access while leaving the client's own data completely untouched, with a repeat detach correctly refused rather than reprocessed. **Clean — every one of this surface's access boundaries held**, standing in useful contrast to the previous journey's finding in the same area of the codebase. Seventy-four journeys of many — several other apps' core journeys remain untraced. (75) Bliss Team Chat (HUD-0119) — channels, DMs, groups, messages, reactions, and a real-time WebSocket push, never traced this arc. Confirmed the CUSTOMER-role block fires before any route logic, the first-access bootstrap channel seeds correctly, a real message triggers a real correctly-attributed notification (not just a 200), and unread counts compute correctly. Attacked the membership boundary from three separate angles: a completely unrelated tenant was refused reading, posting to, or reacting inside a private conversation; a same-tenant colleague who simply wasn't part of that specific conversation was independently refused the same three ways — the more realistic "can I read a colleague's DM" attack; and creating a direct message with a cross-tenant user id was refused outright before anything was created. Confirmed a group created with one valid and one fabricated member id silently keeps only the valid one, and that a non-creator "deleting" a group actually just leaves it intact for everyone else, while the real creator's delete genuinely and permanently removes it. **Clean — every boundary held.** One pre-existing, already-disclosed, and confirmed-inert data leftover was noted for completeness: the dev tenant's own general channel still lists a customer account from before a documented fix, harmless since the customer-role block already prevents any use of it regardless. Seventy-five journeys of many — several other apps' core journeys remain untraced. (76) Bliss Calls/Meetings (HUD-0120) — the platform's real-time WebRTC signaling hub, including a genuinely public, unauthenticated guest-join flow with a password gate and a waiting room, never traced. **Found and fixed a real password-hash disclosure repeated across five different routes**: creating a meeting, cancelling one, ending one, looking one up by its shareable join code, and — the highest-reach case — every single successful join to a password-protected meeting handed the real password hash back to the joining participant, not just the host. Found while driving the ordinary golden path, then confirmed it was five isolated misses against an already-correct pattern used everywhere else in the same file, not a systemic gap. All five fixed and re-verified. **Found and fixed a real bug in the guest-admission flow**: polling the admitted-guest status endpoint re-minted an entirely new guest session on every single poll rather than just the first, silently creating duplicate attendance records for one real person — an easily-triggered bug for a polling design, not a contrived edge case. Fixed with an atomic claim so only the first request to see the admitted state gets to mint a session. Also live-verified the guest-join security boundary broadly: correct password enforcement, waiting-room gating, meeting-locked rejection, and that a guest token from one meeting cannot be reused against a different meeting. Seventy-six journeys of many — several other apps' core journeys remain untraced. (77) Bliss Escalations (HUD-0121) — the small cross-app feature that lets any internal staff member escalate a customs case or a Team Chat message to resolver-tier staff, with a real notification fan-out and a real pending-to-resolved workflow. Confirmed every real resolver-tier user in the tenant was genuinely notified, not just a success response; confirmed two distinct junior staff members each only ever see their own escalations, never each other's; confirmed the full status workflow including that a resolved escalation correctly refuses a further advance; confirmed the chat-escalation's channel-existence check is genuinely tenant-scoped by testing it against a real channel that exists but belongs to a different tenant; and confirmed a real staff member from a completely different tenant can neither read nor advance another tenant's escalation by its real id. **Clean — every boundary held.** Seventy-seven journeys of many — several other apps' core journeys remain untraced. (78) Calendar's public Calendly-style booking pages (HUD-0122) — a genuinely public, unauthenticated scheduling surface where booking a slot creates a real event on a staff member's own calendar. Never traced. The code's own comment explicitly claims the exact race condition this journey tested for is already handled. Confirmed the ordinary sequential case works exactly as claimed — book a slot, immediately trying to book the same one again is correctly refused. Then fired two genuinely concurrent requests at the same never-before-booked slot, not a simulated test: both succeeded, creating two separate real double-booked events for the same host at the same time, both people receiving a real confirmation email. Reproduced it a second and a third time, including a three-way race, before touching any code, to rule out a fluke — it reproduced every time. **Found and fixed a real double-booking race condition**: the "re-check the slot" guard was a plain read-then-write with no lock behind it. Fixed using the same locking pattern already established elsewhere in this codebase for this exact shape of problem. Reran the original two-way race and a fresh three-way race after the fix — in both cases exactly one request succeeded and every other one got a clean, already-existing "someone just booked this" error, with the database confirming exactly one real event at the contested time. Seventy-eight journeys of many — several other apps' core journeys remain untraced. (79) Notes app (HUD-0123) — a real per-note ACL model (team/private/shared visibility, per-user view/edit shares, self-escalation prevention so only a note's creator can change who it's shared with, optimistic locking, and full version history with restore). Read the service file in full specifically hunting for the same "crash instead of 404" bug shape found ~70 times earlier this arc (HUD-0097/0099) — that original sweep only ever looked inside `apps/api/src/routes`, never a `services/*.ts` file, the same blind spot that let `seal-billing.service.ts` through once already. Found it a second time: four call sites — the shared existence/ACL check every read and edit path relies on, updating a note, restoring an old version, and renaming a label — all crashed with a raw, unhandled error instead of a clean 404 when given a nonexistent, deleted, or cross-tenant id. Live-reproduced all five affected routes with real requests before touching any code. **Found and fixed a real bug, the same class as HUD-0097/0099, inside a services file the original sweep never covered.** Fixed with the same existence-check-then-typed-error pattern already established for this bug class, then re-verified every original reproduction now returns a clean 404 — and, as a deliberate regression check, confirmed the fix didn't blur the 404-vs-403 boundary: a same-tenant colleague with no share still correctly gets 403 on a private note, a view-only share still correctly gets 403 attempting to edit, and an edit-share still correctly succeeds. Seventy-nine journeys of many — several other apps' core journeys remain untraced. (80) Tasks + Calendar (HUD-0124) — the platform's richest per-resource ACL model outside of Notes: a task's access level (owner/assignee/editor/viewer) is resolved through four independent, overlapping grant mechanisms (list ownership, a single assignee, shared-list roles, and a plural collaborator roster), plus Calendar's events/freebusy/ICS/booking-pages surface. Live-verified the full permission matrix with real accounts: a viewer sees but cannot write, an editor can edit content but not reassign/move/delete, an assignee gets work access with zero list share or project membership, revoking a share immediately removes all access, and a cross-tenant account is correctly refused both reading and deleting a real task by id. **Found and fixed a real bug**: nine different actions (subtask/comment/collaborator/dependency edits, timer start/stop) all returned the same misleading 404 "Task not found" for a task a viewer could plainly see, instead of a specific 403 explaining they lack write access — the sibling single-task edit route already got this right, these nine hadn't. Fixed to match. **Found and documented (not fixed) a real functional gap**: Calendar's guest-invite feature sends a real, correctly-worded notification when someone is invited to a meeting, but that person's own calendar view never shows the event, their free/busy never reflects it, and no accept/decline mechanism exists anywhere — confirmed live that the notification's own "view it on your calendar" link leads nowhere. Left as a documented gap rather than a partial fix, since a correct fix needs a coordinated backend-and-frontend design decision, not a backend-only patch. **Closed same-day as a user-directed follow-up (HUD-0131)**: extended the event query and the free/busy query with a JSONB-containment "owner or guest" filter, so a guest's own calendar and free/busy now genuinely include events they're invited to, not just ones they organize; added a real accept/decline endpoint (`PATCH /events/:id/rsvp`) since the guest schema's own `status` field could previously never be written to by anyone; and updated the frontend so a guest-visible event renders read-only with Accept/Decline in place of Edit, with drag-reschedule/resize blocked the same way a synthetic holiday row already was. Live-verified end to end against an isolated API instance with three real same-tenant accounts (organizer, invited guest, uninvolved third party) — the guest's listing now includes the event correctly tagged `is_organizer: false`, the uninvolved party's listing correctly excludes it and its RSVP attempt correctly 403s, the guest's own attempt at a full edit still correctly 404s (visibility was extended, write access deliberately was not), and free/busy correctly drops the block once the guest declines. Eighty journeys of many — several other apps' core journeys remain untraced. (81) SMS app (HUD-0125) — a real external-gateway integration (live Africa's Talking + Twilio) with two genuinely public, unauthenticated webhook surfaces: delivery-status callbacks and inbound-message callbacks, the latter auto-recording a real opt-out on any "STOP" reply. **Found and fixed a real, unauthenticated HIGH vulnerability**: neither webhook validated any signature or secret, and the value used to identify which tenant a callback belongs to — a registered sender ID — is public by design, since it's literally what every SMS recipient sees as the "from" on every text that tenant has ever sent. Live-exploited with a single unauthenticated curl request: forged an inbound "STOP" reply from a completely fabricated phone number and confirmed it created a real, permanent opt-out blocking that number from ever receiving SMS from the tenant again, with zero credentials of any kind. Fixed with the same shared-secret-as-query-parameter approach this codebase already uses for its other unsigned webhook provider (GPSWOX), verified against a genuinely separate, isolated instance so the exploit-and-fix testing never touched the shared server. **Found and fixed a second real bug** while verifying the platform's own compliance claim that opted-out numbers are never messaged: the check compared phone numbers as exact strings, so a number opted out as "+255700000111" could still be messaged as "255700000111" or "0700000111" — live-reproduced both bypasses reaching a real outbound API call. Fixed by reusing an existing phone-normalization helper (previously private to an unrelated fraud-detection feature) to match on a canonical form instead, with a small migration to store it. Eighty-one journeys of many — several other apps' core journeys remain untraced. (82) Hudumika AI (HUD-0126) — the cross-app AI assistant: conversational chat with an agentic tool-calling loop that lets the model call real, read-only tenant-data tools to ground its answers, plus per-user/workspace-shared memory and persisted conversation history. A natural place to look for a scoping mistake, since the model rather than a fixed route decides what to call and with what arguments. Verified the tool-calling layer's own claim that no tool call can reach another tenant's data — every tool takes only free-text search parameters, never a raw id, and every query is tenant-scoped. Live-tested conversation ownership from every angle with two real staff accounts: reading, deleting, and even attempting to hijack an existing thread by supplying its real id as a different user all correctly refused; repeated cross-tenant, also refused. Tested memory sharing both directions: a personal fact was correctly invisible and undeletable by a second user, a workspace-shared fact was correctly visible within the tenant and correctly absent across tenants. Re-confirmed the tenant's AI provider API key is still masked in the settings response (fixed in an earlier pass this arc) rather than assuming no regression. **Clean — every boundary held**, no code changes this pass. Eighty-two journeys of many — several other apps' core journeys remain untraced. (83) HuduBI (HUD-0127) — the tenant's own executive dashboard and configurable widget/report builder, whose own header comment explicitly disclaims the product's earlier fabricated numbers ("$28.4M revenue, 8,420 customers, Snowflake sources") and claims every figure is now a live aggregate over the tenant's real rows. Verified that claim live rather than trusting it, confirming the dashboard, analytics, and widget builder all run real tenant-scoped SQL against a hardcoded metric registry, never a user-controlled table or column name. **Found and fixed the same "crash instead of 404" bug class this arc has now found 3 times in different corners of the codebase**: a malformed customer id crashed a cross-app entity-lookup route with a raw 500, and three widget-by-id routes leaked Postgres's own internal error text ("invalid input syntax for type uuid...") straight to the client instead of a clean 404. Fixing it surfaced a second, sharper bug: the file's own existing uuid-checking helper only validated the first two segments of a UUID, not the full shape, so a crafted id with a valid-looking prefix but a garbage tail still slipped through and still crashed — live-proved this wasn't a theoretical nitpick before fixing it with the complete, correctly-anchored pattern already used elsewhere in the codebase. Also adversarially tested a manually-tenant-filtered (not RLS-backed) cross-app customer lookup with a real cross-tenant account — held correctly today, flagged as worth watching. Eighty-three journeys of many — several other apps' core journeys remain untraced. (84) CMS Enterprise collaboration (HUD-0128) — a brand-new module built this week whose entire premise is identity-gated collaboration: assign a specific reviewer, track who requested what, attribute who wrote which editorial comment. Found a real, systemic authorization bypass: four separate functions — deciding a review, cancelling a review, editing an editorial comment, deleting one — each accepted the caller's own id as a parameter but never once compared it against who the action was actually supposed to belong to. The id was only ever used to stamp a record with who did it, never to check whether they were allowed to. Live-exploited, not just read: had one real staff account request a review assigned to a second, then had a completely uninvolved third account successfully approve it, with the decision genuinely persisting. Separately had one account post a real comment, then had a different, uninvolved account overwrite its content and delete it outright, both confirmed to actually happen via a fresh read. **Found and fixed a real cross-user privilege-escalation bug** by adding the missing ownership checks, reusing the exact admin-override pattern this same file already established elsewhere rather than inventing a new one. Re-verified live with a properly controlled test: an uninvolved third party is now correctly refused (403), the actually-assigned reviewer or comment author still succeeds with no regression, and an admin can still moderate content they didn't personally write, exactly as designed. Eighty-four journeys of many — several other apps' core journeys remain untraced. (85) CMS Content Model Builder (HUD-0129) — the no-code system letting a tenant define its own content type with typed fields, including a real foreign-key-like "relation" field pointing at another model, plus a genuinely public dynamic-template route and a custom per-role capability matrix. Chosen specifically because a dynamic, user-defined schema system with a real relation field is exactly the shape of feature where a cross-tenant reference-injection bug tends to hide. Created a real model and relation field on one tenant, then attempted to save an entry referencing a genuinely different tenant's real entry id as the relation value — correctly refused. Confirmed a real draft entry never appears in the public listing and is refused both with no preview token and with a fabricated one, while a genuine token works and — the more meaningful check — doesn't transfer to a different entry's slug, confirming it's bound to the specific row, not just valid-looking. Live-tested the custom capability system by having a real admin revoke a role's content-management rights, confirming the affected account was immediately blocked from creating content while still able to view it, then confirming access returned cleanly after resetting the override. **Clean — every boundary held**, no code changes this pass. Eighty-five journeys of many — several other apps' core journeys remain untraced. (86) CMS Forms + a systemic sweep of the new CMS codebase (HUD-0130) — started as a trace of the public form-submission endpoint (honeypot-guarded, only declared fields ever read, notification email properly escaped), then deliberately widened after noticing every CMS route file shares one local error-catching pattern that quietly bypasses the platform's own new global driver-error-sanitizing handler by forwarding the raw error message directly. Grepped every new CMS service file for the same "crash instead of 404" shape this arc has now found three times before, and found it a 4th time — the widest single pass yet: 7 real bugs across 3 files, every single-resource edit route in Forms, the Content Model Builder, and Webhooks, plus the one route where a caller-supplied id feeds a live outbound HTTP call. A wrong or stale id on any of these returned Kysely's own raw "no result" text at the wrong status code instead of a clean 404 — live-reproduced before writing any fix. **Found and fixed all 7** with the same existence-check-then-typed-error pattern used throughout this arc, re-verified live that every one now returns a specific, correct 404, and that a fresh create-then-edit round trip on all six affected resource types (plus a genuine outbound webhook test) still works with zero regression. Eighty-six journeys of many — several other apps' core journeys remain untraced. **Since this cell was last written, tracing continued through journey 97** (HUD-0097 through HUD-0130 covered the platform-wide "crash instead of 404" bug-class sweep, HuduBI, CMS's enterprise/content-model/forms buildout, and the Developer Platform's CRITICAL cross-account bypass — full detail lives in `AUDIT_REGISTER.md`, not repeated here to avoid this cell growing without bound). Then, on direct user instruction, five previously documented-not-fixed findings from across the whole arc were closed in one batch rather than left open indefinitely: (HUD-0131) Calendar's guest-invite visibility gap — a guest's own calendar/free-busy now genuinely reflect events they're invited to, plus a real accept/decline endpoint; (HUD-0132) Demurrage's `liable_party`/`WAIVED` fields, previously silently no-op'd by `PATCH`, now genuinely persist, with a new guard blocking the change once a charge is actually posted to the GL; (HUD-0133) Ondi's dead `SUPER_ADMIN` bypass on `/org/roles*`, made reachable by adding the role to all 5 routes' gate, matching the sibling `/org/groups*` section that already had it; (HUD-0134) a supplier's "blocked" status, previously cosmetic, now hard-refuses new bills and purchase orders; (HUD-0135) Inventory receipts/count-corrections, previously invisible to the GL, now post against two new accounts (Goods Received Not Invoiced, Inventory Shrinkage/Write-off) backfilled to every tenant; (HUD-0136) the Developer Platform's usage-metering, root-caused live this time (a query was reading a join table's own id instead of the real provider's, silently failing every gateway call's metering insert) and its fabricated verification products, now either genuinely real (landed-cost via the platform's own duty engine, digital seals via real persisted digests) or honestly disclosed as inconclusive when a live BRELA lookup can't be confirmed, instead of invented company data. (97) Fleet Tracking (HUD-0137) — real per-vehicle GPS device authentication (a wrong or missing device secret correctly refused, the secret itself confirmed to never leak back through the vehicle list) and a shared geofence ENTER/EXIT detector reused by both the direct-device-ingestion path and the GPSWOX sync job. **Found and fixed a real MEDIUM gap**: the alerts page's own subtitle promises "geofence breach" alerts, and the database column has documented that exact value as valid since the table was created — but nothing anywhere ever wrote one; the detector correctly recorded every zone crossing internally, it just never told anyone. Fixed by writing a real alert on every crossing (elevated severity for a restricted zone), re-verified live against both a synthetic test zone and a genuine pre-existing tenant geofence the test vehicle happened to also cross. Also found, and deliberately left undecided, that "speeding" alerts (the alerts page's other promised category) have no speed-limit value anywhere in the schema to compare against — a threshold is a product decision, not a wiring fix. Several other apps' core journeys remain untraced |
| Data Integrity & DB | 10% | 70% | RLS solid; migration drift now fixed+proven (HUD-0009); constraints/precision/concurrency unaudited |
| API & Integrations | 10% | 45% | 195 routes not contract-checked; 9 integrations unaudited |
| Auth / RBAC / Tenancy | 10% | 70% | tenancy PASS; auth spot PASS; **HUD-0024's route-by-route RBAC sweep is now CLOSED — all 196 route files read and triaged at least once, 105 gaps found+fixed live (3 CRITICAL), 91 files confirmed already correctly built.** Highlights: HUD-0036 (38 files, the largest batch) found nearly all of Finance/Fleet/HR/Ondi-identity-admin had only a plan-entitlement gate, not a role gate; HUD-0037 found 4 more SEAL-family siblings the original HUD-0031 sweep missed (the "shared URL prefix ≠ shared guard" trap recurring a third time) plus a full-CRUD gap on delivery documents and a quotations IDOR; HUD-0038/0039 closed the remainder, the last ~45 files mostly already correct (the CRM family, onsite-agency and superadmin families, a carefully OTP-gated raw-SQL tool). **Closing this sweep is not the same as proving RBAC is airtight** — the method asked one repeatable question (role gate present or absent) of every file once; it cannot see a role check that's present but wrong, or a flaw spanning multiple routes/services/the RLS layer. Phase 17 (independent adversarial pass) and Phase 5 (functional tracing) remain the next real check on that, and Phase 5 already found one: HUD-0058 (Phase 5's sixteenth journey) found `quotations.routes.ts`'s `PATCH /:id/status` — a *different* route in the same file HUD-0037 already fixed a quotations IDOR in — had no role check at all, live-confirmed as a real cross-customer bypass (a `CUSTOMER` JWT correctly refused a `GET` on another customer's quote could nonetheless silently approve/reject it). Fixed with the same `QUOTE_WRITE_ROLES` guard its sibling routes already use. Proof that "this file has a role gate" (HUD-0024's per-file question) does not mean every mutating route in it does — exactly the residual risk this row already named. Phase 5's twenty-ninth journey found a second, milder instance of the same shape: HUD-0070 found `store.routes.ts`'s `POST /apps` — in the same file whose `STORE_MGMT_ROLES` guard already fixed the install/uninstall routes — had no role check at all, live-confirmed that a `CUSTOMER`-portal account could submit a listing into the platform's marketplace review queue. Fixed with the same CUSTOMER-block convention used everywhere else on the platform |
| Reliability / Error handling | 8% | 55% | HUD-0021 (process-crashing pool bug) found+fixed; broader error-handling sweep not done |
| Testing & Regression | 8% | 20% | HUD-0005 open, but CI (HUD-0006) now runs the tests that do exist on every push |
| Performance | 7% | 40% | not measured |
| Responsive / Mobile | 5% | 40% | not tested |
| UI / UX | 5% | 55% | design-system exists + enforced by CLAUDE.md; not forensically audited |
| Accessibility | 3% | 35% | not audited |
| DevOps / CI/CD / Observability | 4% | 65% | HUD-0006 fixed (real CI); HUD-0022 (lint) still open |
| **Overall** | 100% | **≈ 55%** | |

### Mandatory gates

| Gate | State |
|------|-------|
| CRITICAL security = 0 | ✅ PASS — two found+fixed live this session (HUD-0058, a cross-customer quotation-approval bypass; HUD-0117, a cross-account/cross-tenant authorization bypass on the Developer Platform), zero known open |
| CRITICAL data = 0 | ✅ PASS |
| CRITICAL functional = 0 | ◑ PARTIAL — Phase 5 has now traced 16 golden paths live (stale "not run" note below corrected); zero CRITICAL-functional (crash/data-loss) findings among them, but most of the platform's core journeys remain untraced |
| HIGH security/tenancy/authz = 0 | ❌ 4 open (HUD-0004/0005/0007/0008) — HUD-0024 (RBAC route sweep) is now CLOSED, corrected from an earlier stale count in this row |
| Tenant isolation | ✅ PASS (schema layer, verified) |
| Authentication | ◑ PASS (spot) |
| Authorization | ◑ **PARTIAL** — HUD-0024's route-by-route sweep is CLOSED: all 196 route files triaged at least once, 105 gaps found+fixed live (3 CRITICAL), 91 confirmed already correct, zero known open from that method. Still PARTIAL, not PASS: the sweep proves "role gate present or absent" once per file, not that a present check is *correct*, nor that no flaw spans multiple routes/services — that's what Phase 5 (functional tracing, in progress) and Phase 17 (adversarial pass, not started) are for |
| Core workflows E2E | ◑ PARTIAL — 16 golden paths traced live with real DB verification (Phase 5); most apps' core journeys still untraced |
| Production build | ✅ **PASS** — was broken (HUD-0020), fixed and verified |
| DB migrations reproduce prod | ✅ **PASS** — proven on a from-scratch database (HUD-0009) |
| CI meaningful | ✅ **PASS** — real gate added (HUD-0006) |
| API resilient to a dropped DB connection | ✅ **PASS** — was FAIL, crashed live during this audit (HUD-0021) |
| No production mocks | ❌ UNVERIFIED |
| No exposed secrets (client) | ✅ PASS |

## 5. Recommended next order

1. ~~HUD-0009~~ — **done.** Migration history drift fixed and proven against a
   from-scratch database (0-diff vs. live).
2. ~~HUD-0005 test #1~~ — **done.** Permanent tenant-isolation regression test
   (`tenant-rls-coverage.test.ts`, 7 assertions) added and passing.
3. ~~HUD-0006~~ — **done.** Real CI (`ci.yml`): typecheck, fresh-DB migrate, API tests
   against that fresh DB, full build. Proven locally end to end before being trusted.
4. ~~HUD-0020~~ — **done.** `npm run build` was broken (`packages/ui` couldn't compile);
   fixed, all 4 workspaces build clean.
5. ~~HUD-0021~~ — **done.** Found live mid-audit: an unhandled `pg` pool error crashed
   the whole API on any dropped DB connection. Fixed on all 4 connection pools.
6. ~~HUD-0023~~ — **done, partial Phase 14.** Automated triage of all 195 route files
   found ~300 mutating-endpoint candidates with no role/ownership check across 58 files;
   the highest-stakes one (Onsite — servers/domains/deploy secrets) was read end to end
   and proven live: a `CUSTOMER`-role JWT could create and delete infrastructure. Fixed,
   re-verified live (403 for low-privilege roles, 200 unchanged for admin roles).
7. ~~HUD-0025 / HUD-0026~~ — **done.** ComplyOS Legal Marketplace (create/cancel legal
   engagements, message a law firm, flip milestone payment status) and the shared
   identity-avatar endpoint (any user could overwrite anyone else's photo or a company's
   logo, platform-wide) both had the same shape of gap. Both fixed, both proven live —
   including confirming self-service avatar editing (the real product feature) still works.
   `billing.routes.ts` was checked and found already correctly gated;
   `seal-billing.routes.ts` and `org.routes.ts POST /claim` were checked and are
   deliberately open by design (draft-only invoice, one-time-code capability token).
8. ~~HUD-0027 / 0028 / 0029 / 0030~~ — **done.** Sign (ownership, not role — void/resend/
   retitle/edit any other user's envelope), Contacts and CMS/OneSite (both fully reachable
   by a `CUSTOMER`-role portal account), and Onsite Backups (the exact HUD-0023 gap,
   missed because it's a separate route file) all fixed and proven live. Discovered that
   "does a `CUSTOMER` JWT even reach this app's base route" is a fast, high-confidence
   signal — 4 of 4 apps checked that way turned out to be real bugs — and used it to probe
   the remaining flagged files quickly.
9. ~~HUD-0031~~ — **done.** That probe's 11 flagged candidates turned into 21 confirmed
   gaps once each file was actually read (several apps had sibling plugin files — seal-*
   (8), inventory-* (3), hr/nexushr, declarations/declaration-ledger-anchor — carrying the
   identical gap independently). All fixed with the same minimal, additive per-file hook;
   `org.routes.ts` got the stricter `role === 'ORG'` check instead, since every route in it
   assumed that token shape with no runtime check. Re-verified live in one batch: every
   endpoint now 403s for `CUSTOMER`, unaffected for legitimate internal roles. Found one
   non-auth bug as a side effect — Project OS's `GET /portfolios` 500s for any real user,
   not just `CUSTOMER` — logged as HUD-0032 and left alone (active concurrent development).
10. ~~HUD-0033~~ — **done, second pass after a session boundary.** Re-verified HUD-0001–0031
    all survived intact (an automated commit had captured everything) before continuing. Found
    the audit's most serious result: `sign-stamps.routes.ts`'s `PUT`/`DELETE /stamps/tenant`
    (the tenant's one official e-signature stamp) had zero check — a real forgery vector, and
    the file already had the right role constant declared but never wired to these two routes.
    `sign-versions.routes.ts` turned out to be a whole sibling file of `sign.routes.ts` with no
    entitlement or ownership check at all, missed by HUD-0027 — fixed by exporting and reusing
    that file's own ownership check rather than duplicating it. Six more apps (Customs, AI
    memory, CargoTracker, HuduBI, HuduFreight cargo-loading/warehouse) were CUSTOMER-reachable
    and fixed the same way as HUD-0031. Six files checked and confirmed already correct,
    including `notifications.routes.ts`'s existing `customer_id` branch for `role === 'CUSTOMER'`
    — proof this pass isn't rubber-stamping fixes onto files that don't need them.
11. ~~HUD-0024 remainder~~ — **done (HUD-0035–0039).** All 196 route files now triaged at
    least once; 105 files fixed (3 CRITICAL), 91 confirmed already correct. See HUD-0024's own
    entry for the closure note on what this method does and does not prove.
12. ~~Phase 6 remainder~~ — **done (HUD-0040/0041/0042).** SSRF (4 fixed), log-secret scan
    (clean), path traversal/file access (1 fixed), SQL/command injection (clean). Webhook-
    receiver signature verification on the SMS delivery-status callbacks was found and
    documented but not fixed (needs a live Twilio sandbox to test against safely).
13. **Phase 5 (next)** — functional tracing of the core journeys per app (mandatory gate:
    Core workflows).
13. **HUD-0004 / HUD-0008** — finish non-tenant table classification.
14. **HUD-0022** — wire up real linting (currently completely non-functional).
15. **HUD-0019** — decide the fate of the two dead marketplace-request tables.
16. Phases 7–12 (data integrity, UI/UX, responsive, a11y, performance, integrations).
17. Phase 17 — independent adversarial pass.

## 6. Risks carried

- Concurrent development is active on this branch (multiple `apps/api` files modified
  outside this audit's scope during the session). Re-run the RLS scan after any
  migration lands to catch a new `tenant_id` table shipped without a policy.
- The `text`→`uuid` `tenant_id` migration (HUD-0007) touches live compliance data and
  needs a deliberate, tested rollout.
- No load/volume testing has been done; all evidence is against a dev dataset
  (largest table `api_usage_events` ≈ 666k rows).
