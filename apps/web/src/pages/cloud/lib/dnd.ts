/** Internal drag payload mimetype for move-into-folder — distinguishes an
 *  in-app item drag (row/card → folder) from an OS file drop (upload). */
export const DND_TYPE = 'application/x-fileitem';
